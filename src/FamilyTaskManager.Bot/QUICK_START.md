# Quick Start Guide - Telegram Bot

## Предварительные требования

1. .NET 9.0 SDK
2. PostgreSQL 15+
3. Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Шаг 1: Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Family Task Manager")
   - Введите username бота (должен заканчиваться на "bot", например: "family_task_manager_bot")
4. Сохраните полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройка базы данных

### Вариант 1: Локальный PostgreSQL

```bash
# Создайте базу данных
createdb FamilyTaskManager

# Или через psql
psql -U postgres
CREATE DATABASE FamilyTaskManager;
\q
```

### Вариант 2: Docker

```bash
docker run --name family-task-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=FamilyTaskManager \
  -p 5432:5432 \
  -d postgres:15
```

## Шаг 3: Конфигурация бота

### Вариант 1: User Secrets (Рекомендуется для разработки)

```bash
cd src/FamilyTaskManager.Bot

# Установите токен бота
dotnet user-secrets set "Bot:BotToken" "YOUR_BOT_TOKEN_HERE"
dotnet user-secrets set "Bot:BotUsername" "your_bot_username"

# Настройте строку подключения (если отличается от дефолтной)
dotnet user-secrets set "ConnectionStrings:DefaultConnection" "Host=localhost;Database=FamilyTaskManager;Username=postgres;Password=postgres"
```

### Вариант 2: appsettings.json (Не рекомендуется для production)

Отредактируйте `appsettings.json`:

```json
{
  "Bot": {
    "BotToken": "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz",
    "BotUsername": "your_bot_username"
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=FamilyTaskManager;Username=postgres;Password=postgres"
  }
}
```

⚠️ **Важно**: Не коммитьте токен бота в Git!

## Шаг 4: Применение миграций

```bash
# Из корня решения
cd src/FamilyTaskManager.Infrastructure

# Применить миграции
dotnet ef database update --startup-project ../FamilyTaskManager.Web
```

Или если используете Aspire:

```bash
# Запустите Aspire Host, он автоматически применит миграции
cd src/FamilyTaskManager.AspireHost
dotnet run
```

## Шаг 5: Запуск бота

### Вариант 1: Standalone

```bash
cd src/FamilyTaskManager.Bot
dotnet run
```

Вы должны увидеть:

```
[INF] Starting Telegram Bot...
[INF] Bot started: @your_bot_username
[INF] Starting Telegram Bot Worker
```

### Вариант 2: С Aspire (Рекомендуется)

```bash
cd src/FamilyTaskManager.AspireHost
dotnet run
```

Откройте браузер на http://localhost:15000 для мониторинга.

## Шаг 6: Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота по username (например: @family_task_manager_bot)
3. Отправьте команду `/start`
4. Следуйте инструкциям бота

### Базовый сценарий тестирования

1. **Регистрация**
   ```
   /start
   ```
   Ожидаемый результат: Приветствие и предложение создать семью

2. **Создание семьи**
   - Нажмите кнопку "➕ Создать семью"
   - Введите название семьи (например: "Моя семья")
   - Ожидаемый результат: Семья создана, отображается главное меню

3. **Создание питомца**
   ```
   /pet
   ```
   - Нажмите "➕ Создать питомца"
   - Выберите тип (🐱 Кот / 🐶 Собака / 🐹 Хомяк)
   - Введите имя питомца (например: "Мурзик")
   - Ожидаемый результат: Питомец создан

4. **Просмотр задач**
   ```
   /tasks
   ```
   Ожидаемый результат: Список активных задач (если есть)

5. **Просмотр статистики**
   ```
   /stats
   ```
   Ожидаемый результат: Лидерборд семьи

6. **Справка**
   ```
   /help
   ```
   Ожидаемый результат: Список доступных команд

## Troubleshooting

### Ошибка: "Bot token is invalid"

- Проверьте правильность токена
- Убедитесь, что токен не содержит лишних пробелов
- Проверьте, что токен установлен в правильном формате

### Ошибка: "Connection refused" (PostgreSQL)

- Убедитесь, что PostgreSQL запущен
- Проверьте строку подключения
- Проверьте, что база данных создана

### Ошибка: "Table does not exist"

- Примените миграции: `dotnet ef database update`
- Проверьте, что миграции применились успешно

### Бот не отвечает на команды

- Проверьте логи в консоли
- Убедитесь, что бот запущен
- Проверьте, что у бота нет конфликтов (не запущено несколько экземпляров)

### Ошибка: "User not found"

- Отправьте `/start` для регистрации
- Проверьте, что пользователь создан в БД

## Полезные команды

### Просмотр логов

```bash
# Логи выводятся в консоль
# Для сохранения в файл используйте перенаправление
dotnet run > bot.log 2>&1
```

### Очистка базы данных

```bash
# Удалить все данные
dotnet ef database drop --startup-project ../FamilyTaskManager.Web

# Создать заново
dotnet ef database update --startup-project ../FamilyTaskManager.Web
```

### Проверка состояния бота

```bash
# Отправьте боту команду
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe"
```

## Следующие шаги

1. Изучите [IMPLEMENTATION_STATUS.md](./IMPLEMENTATION_STATUS.md) для понимания текущего состояния
2. Прочитайте [README.md](./README.md) для деталей архитектуры
3. Изучите код в папке `Handlers/` для понимания логики обработки команд
4. Ознакомьтесь с [ТЗ MVP1](../../docs/MVP1/ТЗ%20MVP1.md) для полного понимания требований

## Разработка

### Добавление новой команды

1. Создайте обработчик в `Handlers/Commands/`
2. Зарегистрируйте в `Program.cs`
3. Добавьте маршрутизацию в `CommandHandler.cs`
4. Добавьте в `/help`

### Добавление нового callback-действия

1. Добавьте обработчик в `CallbackQueryHandler.cs`
2. Создайте inline-кнопку с соответствующим callback_data
3. Протестируйте

### Отладка

Используйте точки останова в Visual Studio / VS Code:

```csharp
// В CommandHandler.cs
public async Task HandleCommandAsync(...)
{
    // Точка останова здесь
    var telegramId = message.From!.Id;
    ...
}
```

## Production Deployment

Для production рекомендуется:

1. Использовать переменные окружения вместо User Secrets
2. Настроить Webhook вместо Long Polling
3. Использовать Redis для сессий вместо In-Memory
4. Настроить мониторинг и алерты
5. Использовать Docker для деплоя

См. документацию по деплою (TODO).

# PR Preview Environments

Автоматическое развертывание preview окружений для Pull Request'ов.

## 📋 Содержание

- [Что это такое](#что-это-такое)
- [Как использовать](#как-использовать)
- [Настройка](#настройка)
- [Архитектура](#архитектура)
- [Управление](#управление)
- [Troubleshooting](#troubleshooting)

---

## Что это такое

**PR Preview Environment** — это отдельное изолированное окружение для каждого Pull Request, которое позволяет:

- ✅ Тестировать изменения перед слиянием в main
- ✅ Проверять работу Telegram бота с реальными данными
- ✅ Изолировать тестовые данные от production
- ✅ Автоматически очищать ресурсы после закрытия PR

### Особенности

- 🔒 **Изолированное окружение** для каждого PR
- 🤖 **Отдельный Telegram бот** для тестирования
- 🗄️ **Отдельная база данных** (не влияет на production)
- 🐳 **Уникальные контейнеры** и порты
- 🧹 **Автоматическая очистка** при закрытии PR

---

## Как использовать

### Шаг 1: Создайте Pull Request

```bash
git checkout -b feature/my-new-feature
git add .
git commit -m "Add new feature"
git push origin feature/my-new-feature
```

Создайте PR на GitHub.

### Шаг 2: Запустите деплой вручную

Перейдите в Actions и запустите workflow:

```
GitHub → Actions → Deploy to VPS via Private Registry → Run workflow
```

Заполните параметры:

- **Branch:** Выберите вашу PR ветку
- **Deployment environment:** Выберите `pr-preview`
- **PR number:** Введите номер вашего PR (например: `123`)

Нажмите **"Run workflow"**

### Шаг 3: Дождитесь деплоя

GitHub Actions автоматически:

1. ✅ Запустит тесты
2. 🔨 Соберет Docker образ
3. 📤 Отправит в registry с тегом `pr-{number}`
4. 🚀 Развернет preview окружение на VPS

**Время:** ~4-5 минут

### Шаг 4: Тестируйте

После успешного деплоя бот оставит комментарий в PR с деталями:

```
🚀 Preview Environment Deployed

Environment Details:
- 🤖 Telegram Bot: @your_pr_bot
- 🗄️ Database: FamilyTaskManager_PR_123
- 🐳 Container: family-task-manager-pr-123
- 🔌 Port: 8203

How to test:
1. Open Telegram
2. Find bot @your_pr_bot
3. Send /start
4. Test your changes
```

### Шаг 5: Очистка

**Автоматически:**

- При закрытии PR (merge или close)

**Вручную:**

```
GitHub → Actions → Cleanup PR Preview → Run workflow
```

---

## Настройка

### 1. Создайте тестовый Telegram бот

```
1. Откройте @BotFather в Telegram
2. Отправьте /newbot
3. Назовите бота (например: "MyApp PR Preview Bot")
4. Username: your_app_pr_bot
5. Сохраните токен
```

### 2. Создайте GitHub Secrets

Перейдите: `GitHub Repository → Settings → Secrets and variables → Actions`

Добавьте следующие secrets:

#### Для production (уже должны быть)

| Secret              | Описание          |
|---------------------|-------------------|
| `VPS_HOST`          | IP адрес VPS      |
| `VPS_USERNAME`      | SSH пользователь  |
| `VPS_SSH_KEY`       | SSH ключ          |
| `REGISTRY_USERNAME` | Registry user     |
| `REGISTRY_PASSWORD` | Registry password |

#### Для PR Preview (новые)

| Secret                 | Описание                | Пример              |
|------------------------|-------------------------|---------------------|
| `PR_BOT_TOKEN`         | Токен тестового бота    | `123456:ABC-DEF...` |
| `PR_BOT_USERNAME`      | Username тестового бота | `your_app_pr_bot`   |
| `PR_POSTGRES_USER`     | Пользователь БД для PR  | `familytask_pr`     |
| `PR_POSTGRES_PASSWORD` | Пароль БД для PR        | `secure_password`   |

### 3. Настройте VPS

На VPS должно быть достаточно ресурсов для нескольких preview окружений:

**Минимальные требования:**

- **RAM:** 4+ GB (2GB base + 1GB на каждый PR)
- **Disk:** 30+ GB
- **CPU:** 2+ cores

**Рекомендуемые:**

- **RAM:** 8+ GB
- **Disk:** 50+ GB
- **CPU:** 4+ cores

---

## Архитектура

### Структура на VPS

```
/opt/
├── family-task-manager/              # Production
│   ├── docker-compose.prod.yml
│   └── .env
│
├── family-task-manager-pr-123/       # PR #123 Preview
│   ├── docker-compose.pr-preview.yml
│   └── .env
│
├── family-task-manager-pr-456/       # PR #456 Preview
│   ├── docker-compose.pr-preview.yml
│   └── .env
│
└── docker-registry/                  # Private Registry
    └── registry-data/
        └── family-task-manager:pr-123
        └── family-task-manager:pr-456
```

### Изоляция окружений

| Компонент        | Production             | PR #123                       | PR #456                       |
|------------------|------------------------|-------------------------------|-------------------------------|
| **Container**    | `family-task-manager`  | `family-task-manager-pr-123`  | `family-task-manager-pr-456`  |
| **Database**     | `FamilyTaskManager`    | `FamilyTaskManager_PR_123`    | `FamilyTaskManager_PR_456`    |
| **DB Container** | `family-task-postgres` | `family-task-postgres-pr-123` | `family-task-postgres-pr-456` |
| **Network**      | `family-task-network`  | `family-task-network-pr-123`  | `family-task-network-pr-456`  |
| **App Port**     | `8080`                 | `8203` (8080+123)             | `8536` (8080+456)             |
| **DB Port**      | `5432`                 | `5555` (5432+123)             | `5888` (5432+456)             |
| **Bot Token**    | Production token       | PR token                      | PR token                      |
| **Bot Username** | `@prod_bot`            | `@pr_bot`                     | `@pr_bot`                     |

### Workflow процесс

```
┌─────────────────────────────────────────────────────────────────┐
│  Developer                                                      │
│  └── Creates PR → Runs workflow manually                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  GitHub Actions: deploy-registry.yml (pr-preview mode)          │
│                                                                 │
│  1. Setup Environment (determine pr-preview)                    │
│  2. Run Tests ✓                                                 │
│  3. Build Image → Tag: pr-{number}                              │
│  4. Push to Registry                                            │
│  5. Deploy to VPS:                                              │
│     ├── Create /opt/family-task-manager-pr-{number}/            │
│     ├── Copy docker-compose.prod.yml                            │
│     ├── Generate unique .env with PR secrets                    │
│     ├── Modify container names (sed)                            │
│     ├── Start containers with unique names                      │
│     └── Comment on PR with details                             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  VPS: Preview Environment Running                               │
│  - Isolated containers                                          │
│  - Separate database                                            │
│  - Test bot active                                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  Developer Tests in Telegram                                    │
│  └── @pr_bot → /start → Test features                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  PR Closed or Label Removed                                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  GitHub Actions: cleanup-pr-preview.yml                         │
│  - Stop containers                                              │
│  - Remove database                                              │
│  - Delete images                                                │
│  - Remove directory                                             │
│  - Comment on PR                                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## Управление

### Просмотр активных preview окружений

```bash
# На VPS
ssh user@vps

# Список директорий
ls -la /opt/ | grep "pr-"

# Список контейнеров
docker ps | grep "pr-"

# Список баз данных
docker exec family-task-postgres-pr-123 psql -U familytask_pr -l
```

### Просмотр логов

```bash
# Логи конкретного PR
docker logs family-task-manager-pr-123 -f

# Логи БД
docker logs family-task-postgres-pr-123 -f

# Через docker compose
cd /opt/family-task-manager-pr-123
docker compose -f docker-compose.pr-preview.yml logs -f
```

### Перезапуск preview окружения

```bash
cd /opt/family-task-manager-pr-123
docker compose -f docker-compose.pr-preview.yml restart
```

### Обновление preview окружения

Просто сделайте новый commit в PR:

```bash
git add .
git commit -m "Update feature"
git push
```

GitHub Actions автоматически пересоберет и обновит preview.

### Ручная очистка

```bash
# На VPS
cd /opt/family-task-manager-pr-123
docker compose -f docker-compose.pr-preview.yml down -v
cd /opt
rm -rf family-task-manager-pr-123
```

---

## Мониторинг ресурсов

### Проверка использования

```bash
# Использование RAM
docker stats --no-stream | grep "pr-"

# Использование диска
du -sh /opt/family-task-manager-pr-*

# Количество preview окружений
ls -d /opt/family-task-manager-pr-* | wc -l
```

### Лимиты

**Рекомендуется:**

- Максимум 3-5 активных preview окружений одновременно
- Автоматическая очистка после 7 дней неактивности (можно настроить)

### Автоматическая очистка старых preview

Создайте cron job на VPS:

```bash
# Добавьте в crontab
crontab -e

# Очистка preview старше 7 дней
0 2 * * * find /opt/family-task-manager-pr-* -type d -mtime +7 -exec bash -c 'cd {} && docker compose -f docker-compose.pr-preview.yml down -v 2>/dev/null; rm -rf {}' \;
```

---

## Безопасность

### Изоляция

- ✅ Каждый PR имеет свою БД
- ✅ Контейнеры изолированы в отдельных Docker networks
- ✅ Уникальные порты для каждого PR
- ✅ Отдельный Telegram бот (не влияет на production)

### Доступ

- ⚠️ Preview окружения доступны только через Telegram бот
- ⚠️ Порты не открыты наружу (только внутри VPS)
- ✅ Используются те же credentials что и для production деплоя

### Рекомендации

1. **Не используйте production токены** для PR preview
2. **Ограничьте количество** одновременных preview окружений
3. **Регулярно очищайте** старые preview
4. **Мониторьте ресурсы** VPS

---

## Troubleshooting

### Preview не деплоится

**Проблема:** Workflow не запускается

**Решение:**

```bash
# Проверьте наличие label
GitHub PR → Labels → Убедитесь что есть "deploy-preview"

# Проверьте логи workflow
GitHub → Actions → Deploy PR Preview → View logs
```

### Недостаточно ресурсов на VPS

**Проблема:** Контейнеры не запускаются из-за нехватки RAM

**Решение:**

```bash
# Очистите старые preview
cd /opt
for dir in family-task-manager-pr-*; do
  cd "$dir"
  docker compose -f docker-compose.pr-preview.yml down -v
  cd /opt
  rm -rf "$dir"
done

# Или увеличьте RAM на VPS
```

### Конфликт портов

**Проблема:** Порт уже занят

**Решение:**

```bash
# Проверьте занятые порты
netstat -tulpn | grep LISTEN

# Preview использует порты: 8080 + PR_NUMBER
# Если конфликт, измените базовый порт в workflow
```

### Telegram бот не отвечает

**Проблема:** Бот не реагирует на команды

**Решение:**

```bash
# Проверьте логи
docker logs family-task-manager-pr-123 -f

# Проверьте токен в .env
cat /opt/family-task-manager-pr-123/.env | grep BOT_TOKEN

# Проверьте что контейнер запущен
docker ps | grep pr-123
```

### База данных не создается

**Проблема:** Ошибка подключения к БД

**Решение:**

```bash
# Проверьте контейнер PostgreSQL
docker logs family-task-postgres-pr-123

# Проверьте что БД создана
docker exec family-task-postgres-pr-123 psql -U familytask_pr -l

# Пересоздайте контейнеры
cd /opt/family-task-manager-pr-123
docker compose -f docker-compose.pr-preview.yml down -v
docker compose -f docker-compose.pr-preview.yml up -d
```

---

## Примеры использования

### Пример 1: Тестирование новой функции

```bash
# 1. Создайте ветку
git checkout -b feature/new-command

# 2. Внесите изменения
# ... code changes ...

# 3. Commit и push
git add .
git commit -m "Add new /stats command"
git push origin feature/new-command

# 4. Создайте PR на GitHub

# 5. Запустите workflow вручную
# Actions → Deploy to VPS → Run workflow
# environment: pr-preview, pr_number: 123

# 6. Дождитесь деплоя (~5 мин)

# 7. Тестируйте в Telegram
# @pr_bot → /start → /stats

# 8. Если все ОК, merge PR
# Preview автоматически очистится
```

### Пример 2: Тестирование изменений в БД

```bash
# 1. Создайте миграцию
dotnet ef migrations add AddNewField

# 2. Push в PR
git push

# 3. Preview автоматически применит миграцию

# 4. Проверьте БД
ssh user@vps
docker exec family-task-postgres-pr-123 psql -U familytask_pr -d FamilyTaskManager_PR_123
\dt  # Список таблиц
```

### Пример 3: Параллельное тестирование нескольких PR

```
PR #123: feature/new-command     → @pr_bot (port 8203)
PR #124: feature/ui-improvements → @pr_bot (port 8204)
PR #125: fix/bug-in-scheduler    → @pr_bot (port 8205)

Все работают независимо!
```

---

## Лучшие практики

### Когда использовать PR Preview

✅ **Используйте для:**

- Новых функций, требующих тестирования
- Изменений в Telegram боте
- Миграций базы данных
- Интеграционных тестов
- Демонстрации функционала

❌ **Не используйте для:**

- Мелких исправлений (typos, форматирование)
- Изменений только в документации
- Изменений в CI/CD конфигурации

### Workflow

1. **Создайте PR**
2. **Дождитесь прохождения автоматических тестов**
3. **Запустите preview** вручную только если нужно тестирование
4. **Тестируйте** в preview окружении
5. **Обновляйте preview** при необходимости (запустите workflow снова)
6. **Merge PR** когда все готово (preview автоматически очистится)

### Очистка

- Preview автоматически удаляется при закрытии PR
- Не держите preview окружения дольше необходимого
- Регулярно проверяйте список активных preview
- Можно очистить вручную через Cleanup workflow

---

## FAQ

**Q: Сколько preview окружений можно запустить одновременно?**  
A: Зависит от ресурсов VPS. Рекомендуется 3-5 одновременно.

**Q: Можно ли использовать один Telegram бот для всех preview?**  
A: Да, можно использовать один тестовый бот для всех PR.

**Q: Что происходит с данными при закрытии PR?**  
A: Все данные удаляются (контейнеры, БД, образы).

**Q: Можно ли восстановить preview после удаления?**  
A: Нет, но можно заново запустить workflow вручную.

**Q: Влияет ли preview на production?**  
A: Нет, полностью изолированные окружения.

**Q: Можно ли деплоить preview вручную?**  
A: Да, через `workflow_dispatch` в GitHub Actions.

---

## Дополнительные ресурсы

- [GitHub Actions Setup](GITHUB_ACTIONS_SETUP.md)
- [Private Registry Setup](../docs/PRIVATE_REGISTRY_SETUP.md)
- [Deployment Summary](../DEPLOYMENT_SUMMARY.md)

---

**Готово!** Теперь вы можете тестировать изменения в изолированных preview окружениях перед слиянием в main. 🚀

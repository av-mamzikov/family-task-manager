# Руководство по GitHub Actions Workflows

Краткое руководство по использованию автоматических проверок и деплоя.

## 🎯 Что происходит автоматически

### При создании Pull Request

```
1. ✅ Запускаются тесты (tests.yml)
2. ✅ Проверяется качество кода (code-quality.yml)
3. ✅ Вычисляется покрытие кода (code-coverage.yml)
4. 📝 В PR добавляется комментарий с результатами
```

### При Push в main/master

```
1. ✅ Запускаются тесты
2. ✅ Если тесты прошли → сборка Docker образа
3. ✅ Загрузка образа в Docker Hub
4. ✅ Автоматический деплой на VPS
5. ✅ Перезапуск контейнеров
```

## 📝 Workflows

### 1. Tests - Автоматическое тестирование

**Файл:** `.github/workflows/tests.yml`

**Когда запускается:**

- Push в `main`, `master`, `develop`
- Pull Request в `main`, `master`, `develop`

**Что проверяет:**

- Компиляция проекта
- Прохождение всех unit тестов
- Работа с PostgreSQL

**Как посмотреть результаты:**

1. Откройте вкладку `Actions` в GitHub
2. Выберите workflow `Tests`
3. Посмотрите логи и результаты

### 2. Deploy to VPS - Автоматический деплой

**Файл:** `.github/workflows/deploy.yml`

**Когда запускается:**

- Push в `main` или `master`
- Ручной запуск

**Этапы:**

1. **Test** - запуск тестов
2. **Build and Deploy** - сборка и деплой (только если тесты прошли)

**Важно:** Деплой НЕ произойдёт, если тесты провалились!

### 3. Code Coverage - Покрытие кода

**Файл:** `.github/workflows/code-coverage.yml`

**Когда запускается:**

- Push в `main`, `master`, `develop`
- Pull Request в `main`, `master`

**Что делает:**

- Запускает тесты с измерением покрытия
- Генерирует отчёт
- Добавляет комментарий в PR

**Пороги качества:**

- 🟢 > 80% - отлично
- 🟡 60-80% - хорошо
- 🔴 < 60% - требует улучшения

### 4. Code Quality - Качество кода

**Файл:** `.github/workflows/code-quality.yml`

**Когда запускается:**

- Push в `main`, `master`, `develop`
- Pull Request в `main`, `master`

**Что проверяет:**

- Форматирование кода (`dotnet format`)
- Warnings в коде
- .NET analyzers
- Безопасность кода

## 🚀 Типичные сценарии

### Сценарий 1: Разработка новой фичи

```bash
# 1. Создайте ветку
git checkout -b feature/new-feature

# 2. Разработайте фичу
# ... пишете код ...

# 3. Запустите тесты локально
dotnet test

# 4. Проверьте форматирование
dotnet format

# 5. Закоммитьте изменения
git add .
git commit -m "Add new feature"
git push origin feature/new-feature

# 6. Создайте Pull Request на GitHub
# GitHub автоматически запустит все проверки

# 7. Дождитесь успешных проверок
# 8. Мержите PR в main
# 9. Автоматически произойдёт деплой!
```

### Сценарий 2: Hotfix в production

```bash
# 1. Создайте ветку от main
git checkout main
git pull
git checkout -b hotfix/critical-bug

# 2. Исправьте баг
# ... пишете код ...

# 3. Запустите тесты
dotnet test

# 4. Закоммитьте
git add .
git commit -m "Fix critical bug"
git push origin hotfix/critical-bug

# 5. Создайте PR
# 6. После успешных проверок - мерж
# 7. Автоматический деплой
```

### Сценарий 3: Ручной деплой

Если нужно задеплоить без нового коммита:

1. Откройте GitHub → `Actions`
2. Выберите `Deploy to VPS`
3. Нажмите `Run workflow`
4. Выберите ветку (обычно `main`)
5. Нажмите `Run workflow`

## ✅ Чеклист перед Push

Перед тем как запушить код, проверьте локально:

```bash
# 1. Тесты проходят
dotnet test

# 2. Код отформатирован
dotnet format

# 3. Нет warnings
dotnet build /p:TreatWarningsAsErrors=true

# 4. Покрытие кода (опционально)
dotnet test --collect:"XPlat Code Coverage"
```

## 🔍 Как посмотреть результаты

### Статус проверок в PR

В Pull Request внизу отображаются все проверки:

- ✅ Tests - тесты прошли
- ✅ Code Quality - качество OK
- ✅ Code Coverage - покрытие вычислено

### Детальные логи

1. Кликните на `Details` рядом с проверкой
2. Откроется страница с логами
3. Разверните нужный шаг для просмотра деталей

### Артефакты

Некоторые workflows сохраняют артефакты:

1. Откройте workflow run
2. Прокрутите вниз до `Artifacts`
3. Скачайте нужный артефакт (например, test-results)

## 🐛 Что делать если проверка провалилась

### Tests failed

```bash
# 1. Посмотрите логи в GitHub Actions
# 2. Воспроизведите локально
dotnet test

# 3. Исправьте тесты
# 4. Закоммитьте и запушьте
git add .
git commit -m "Fix tests"
git push
```

### Code Quality failed

```bash
# 1. Проверьте форматирование
dotnet format --verify-no-changes

# 2. Автоматически исправьте
dotnet format

# 3. Закоммитьте
git add .
git commit -m "Fix code formatting"
git push
```

### Deploy failed

```bash
# 1. Проверьте логи в GitHub Actions
# 2. Проверьте секреты (DOCKER_USERNAME, VPS_HOST и т.д.)
# 3. Проверьте доступность VPS
ssh root@ваш_vps_ip

# 4. Проверьте логи на сервере
ssh root@ваш_vps_ip "docker compose -C /opt/family-task-manager logs"

# 5. Попробуйте ручной деплой
ssh root@ваш_vps_ip "cd /opt/family-task-manager && bash deploy.sh"
```

## 📊 Мониторинг

### Просмотр истории запусков

1. `Actions` → выберите workflow
2. Увидите список всех запусков
3. Зелёная галочка ✅ - успех
4. Красный крестик ❌ - провал

### Время выполнения

В `Actions` → `Insights` можно посмотреть:

- Среднее время выполнения
- Процент успешных запусков
- Использование минут GitHub Actions

### Уведомления

GitHub отправляет email уведомления:

- При провале проверок в вашем PR
- При провале деплоя в main

Настройки: `Settings` → `Notifications`

## 🎓 Лучшие практики

### 1. Всегда запускайте тесты локально

```bash
dotnet test
```

### 2. Используйте осмысленные commit messages

```bash
# ❌ Плохо
git commit -m "fix"

# ✅ Хорошо
git commit -m "Fix null reference exception in TaskHandler"
```

### 3. Создавайте небольшие PR

- Легче ревьюить
- Быстрее проходят проверки
- Проще откатить при проблемах

### 4. Не игнорируйте warnings

```bash
# Соберите с warnings as errors
dotnet build /p:TreatWarningsAsErrors=true
```

### 5. Следите за покрытием кода

Стремитесь к > 80% покрытия тестами

## 🔗 Полезные команды

```bash
# Локальный запуск всех проверок
dotnet restore
dotnet build --configuration Release
dotnet test --configuration Release
dotnet format --verify-no-changes

# Проверка покрытия
dotnet test --collect:"XPlat Code Coverage"

# Сборка Docker образа (как в CI)
docker build -t family-task-manager:test .

# Просмотр статуса на сервере
ssh root@ваш_vps_ip "docker compose -C /opt/family-task-manager ps"
```

## 📚 Дополнительная информация

- **Полная документация CI/CD:** [CI_CD.md](CI_CD.md)
- **Настройка деплоя:** [../DEPLOYMENT.md](../DEPLOYMENT.md)
- **Шпаргалка по командам:** [DEPLOYMENT_CHEATSHEET.md](DEPLOYMENT_CHEATSHEET.md)

---

**Вопросы?** Создайте issue в репозитории или посмотрите логи в разделе Actions.

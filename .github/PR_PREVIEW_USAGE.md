# 🚀 Использование PR Preview

Краткое руководство по развертыванию PR в preview окружение.

## 📋 Как развернуть PR в preview

### Вариант 1: Через интерфейс GitHub (рекомендуется)

1. **Откройте ваш Pull Request** на GitHub

2. **Перейдите в Actions**
   ```
   GitHub → Pull Request → Actions tab
   ```

3. **Найдите workflow** "Deploy to VPS via Private Registry"

4. **Нажмите "Run workflow"**
    - **Branch:** Выберите вашу PR ветку
    - **Deployment environment:** Выберите `pr-preview`
    - **PR number:** Введите номер вашего PR (например: `123`)

5. **Нажмите "Run workflow"**

6. **Дождитесь завершения** (~5 минут)

7. **Проверьте комментарий** в PR с деталями окружения

### Вариант 2: Через GitHub CLI

```bash
# Установите GitHub CLI если еще не установлен
# https://cli.github.com/

# Запустите workflow для PR #123
gh workflow run deploy-registry.yml \
  -f environment=pr-preview \
  -f pr_number=123 \
  --ref feature/my-branch
```

---

## 🎯 Что происходит при деплое

```
1. ✅ Запускаются тесты
2. 🔨 Собирается Docker образ с тегом pr-{number}
3. 📤 Образ отправляется в Private Registry
4. 🚀 Создается изолированное окружение:
   - Директория: /opt/family-task-manager-pr-{number}
   - Контейнер: family-task-manager-pr-{number}
   - База данных: FamilyTaskManager_PR_{number}
   - Тестовый Telegram бот
5. 💬 Бот комментирует PR с деталями
```

---

## 🧪 Тестирование в preview

После успешного деплоя:

1. **Откройте Telegram**
2. **Найдите тестовый бот** (указан в комментарии к PR)
3. **Отправьте** `/start`
4. **Тестируйте** ваши изменения

### Просмотр логов

```bash
# Подключитесь к VPS
ssh user@vps

# Просмотр логов PR #123
docker logs family-task-manager-pr-123 -f

# Или через docker compose
cd /opt/family-task-manager-pr-123
docker compose -f docker-compose.pr.yml logs -f
```

---

## 🔄 Обновление preview

Если вы внесли новые изменения в PR:

### Автоматически (если настроен push trigger)

```bash
git add .
git commit -m "Update feature"
git push
```

### Вручную

Повторите шаги из раздела "Как развернуть PR в preview"

---

## 🧹 Очистка preview

### Автоматически

Preview окружение **автоматически удаляется** при закрытии PR (merge или close).

### Вручную

1. **Через GitHub Actions:**
   ```
   GitHub → Actions → Cleanup PR Preview → Run workflow
   ```
   Укажите номер PR для очистки

2. **Через SSH:**
   ```bash
   ssh user@vps
   cd /opt/family-task-manager-pr-123
   docker compose -f docker-compose.pr.yml down -v
   cd /opt
   rm -rf family-task-manager-pr-123
   ```

---

## 📊 Мониторинг preview окружений

### Список активных preview

```bash
# На VPS
ssh user@vps

# Список директорий
ls -la /opt/ | grep "pr-"

# Список контейнеров
docker ps | grep "pr-"

# Использование ресурсов
docker stats --no-stream | grep "pr-"
```

### Информация о конкретном preview

```bash
# Статус PR #123
cd /opt/family-task-manager-pr-123
docker compose -f docker-compose.pr.yml ps

# Логи
docker logs family-task-manager-pr-123 --tail=100

# Переменные окружения
cat .env
```

---

## 🔧 Параметры окружения

### Production vs PR Preview

| Параметр         | Production                 | PR Preview                             |
|------------------|----------------------------|----------------------------------------|
| **Директория**   | `/opt/family-task-manager` | `/opt/family-task-manager-pr-{number}` |
| **Контейнер**    | `family-task-manager`      | `family-task-manager-pr-{number}`      |
| **База данных**  | `FamilyTaskManager`        | `FamilyTaskManager_PR_{number}`        |
| **Image tag**    | `latest`                   | `pr-{number}`                          |
| **Bot token**    | `TELEGRAM_BOT_TOKEN`       | `PR_BOT_TOKEN`                         |
| **Bot username** | `TELEGRAM_BOT_USERNAME`    | `PR_BOT_USERNAME`                      |
| **Environment**  | `Production`               | `Staging`                              |

### Переопределяемые параметры

В workflow автоматически переопределяются:

```yaml
# Production
TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
TELEGRAM_BOT_USERNAME: ${{ secrets.TELEGRAM_BOT_USERNAME }}
POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}

# PR Preview
TELEGRAM_BOT_TOKEN: ${{ secrets.PR_BOT_TOKEN }}
TELEGRAM_BOT_USERNAME: ${{ secrets.PR_BOT_USERNAME }}
POSTGRES_USER: ${{ secrets.PR_POSTGRES_USER }}
POSTGRES_PASSWORD: ${{ secrets.PR_POSTGRES_PASSWORD }}
```

---

## ⚠️ Важные моменты

### Ресурсы VPS

- Каждый preview занимает ~500MB RAM
- Рекомендуется не более 3-5 активных preview одновременно
- Мониторьте использование диска

### Изоляция

- ✅ Каждый PR имеет свою БД
- ✅ Отдельные Docker networks
- ✅ Уникальные имена контейнеров
- ✅ Не влияет на production

### Безопасность

- ⚠️ Используйте отдельный тестовый бот
- ⚠️ Не используйте production токены
- ⚠️ Preview окружения доступны только через Telegram

---

## 🆘 Troubleshooting

### Workflow не запускается

**Проблема:** Кнопка "Run workflow" неактивна

**Решение:**

- Убедитесь что вы на странице Actions
- Выберите workflow "Deploy to VPS via Private Registry"
- Убедитесь что выбрана правильная ветка

### Preview не деплоится

**Проблема:** Workflow завершается с ошибкой

**Решение:**

```bash
# Проверьте логи workflow в GitHub Actions
# Проверьте что все GitHub Secrets настроены:
# - PR_BOT_TOKEN
# - PR_BOT_USERNAME
# - PR_POSTGRES_USER
# - PR_POSTGRES_PASSWORD
```

### Бот не отвечает

**Проблема:** Telegram бот не реагирует на команды

**Решение:**

```bash
# Проверьте логи на VPS
ssh user@vps
docker logs family-task-manager-pr-{NUMBER} -f

# Проверьте что контейнер запущен
docker ps | grep pr-{NUMBER}

# Проверьте токен
cd /opt/family-task-manager-pr-{NUMBER}
cat .env | grep BOT_TOKEN
```

### Недостаточно места на VPS

**Проблема:** Ошибка "no space left on device"

**Решение:**

```bash
# Очистите старые preview
ssh user@vps
cd /opt
for dir in family-task-manager-pr-*; do
  cd "$dir"
  docker compose -f docker-compose.pr.yml down -v
  cd /opt
  rm -rf "$dir"
done

# Очистите Docker
docker system prune -a -f
```

---

## 📚 Примеры использования

### Пример 1: Тестирование новой команды бота

```bash
# 1. Создайте ветку и внесите изменения
git checkout -b feature/new-command
# ... код ...
git push origin feature/new-command

# 2. Создайте PR на GitHub

# 3. Запустите preview через GitHub Actions
# Actions → Deploy to VPS → Run workflow
# environment: pr-preview
# pr_number: 123

# 4. Тестируйте в Telegram
# @test_bot → /start → /new-command

# 5. Если нужны изменения
git commit -am "Fix command"
git push
# Запустите workflow снова

# 6. Merge PR когда готово
# Preview автоматически удалится
```

### Пример 2: Тестирование миграции БД

```bash
# 1. Создайте миграцию
dotnet ef migrations add AddNewTable

# 2. Push и создайте PR

# 3. Запустите preview

# 4. Проверьте БД
ssh user@vps
docker exec family-task-postgres-pr-123 \
  psql -U familytask_pr -d FamilyTaskManager_PR_123 -c "\dt"

# 5. Тестируйте функционал с новой таблицей
```

### Пример 3: Демонстрация функционала

```bash
# 1. Разверните preview для PR

# 2. Поделитесь ссылкой на тестовый бот с командой
# "Можете протестировать новую функцию в @test_bot"

# 3. Соберите feedback

# 4. Внесите изменения и обновите preview

# 5. Merge когда все довольны
```

---

## 🎓 Лучшие практики

### Когда использовать preview

✅ **Используйте для:**

- Новых функций бота
- Изменений в БД (миграции)
- Сложных изменений требующих тестирования
- Демонстрации функционала команде

❌ **Не используйте для:**

- Мелких исправлений (typos)
- Изменений в документации
- Изменений только в тестах

### Workflow

1. Создайте PR
2. Дождитесь прохождения автоматических тестов
3. Запустите preview только если нужно ручное тестирование
4. Тестируйте тщательно
5. Обновляйте preview при необходимости
6. Merge PR когда все готово
7. Preview автоматически очистится

### Очистка

- Не держите preview дольше необходимого
- Очищайте вручную если не планируете merge
- Мониторьте количество активных preview

---

## 📞 Поддержка

**Проблемы с деплоем?**

- Проверьте [Troubleshooting](#troubleshooting)
- Посмотрите логи в GitHub Actions
- Проверьте логи на VPS

**Нужна помощь?**

- Создайте issue на GitHub
- Обратитесь к команде

---

## 📖 Дополнительная документация

- [PR Preview Environments](PR_PREVIEW_ENVIRONMENTS.md) - полное руководство
- [PR Preview Quickstart](PR_PREVIEW_QUICKSTART.md) - быстрая настройка
- [GitHub Actions Setup](GITHUB_ACTIONS_SETUP.md) - настройка CI/CD
- [CI/CD Overview](CI_CD.md) - обзор всех workflows

---

**Готово!** Теперь вы можете тестировать PR перед слиянием в main. 🎉

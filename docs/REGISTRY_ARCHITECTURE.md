# Архитектура Private Registry

Визуальное представление работы системы с Private Docker Registry.

## 🏗️ Общая архитектура

```
┌─────────────────────────────────────────────────────────────────────┐
│                           VPS Server                                │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Docker Network                            │  │
│  │                                                              │  │
│  │  ┌─────────────────┐         ┌──────────────────┐          │  │
│  │  │  Private        │         │  Family Task     │          │  │
│  │  │  Registry       │◄────────│  Manager App     │          │  │
│  │  │  :5000          │  pull   │                  │          │  │
│  │  └────────┬────────┘         └────────┬─────────┘          │  │
│  │           │                           │                     │  │
│  │           │                           │                     │  │
│  │  ┌────────▼────────┐         ┌────────▼─────────┐          │  │
│  │  │  Registry       │         │  PostgreSQL      │          │  │
│  │  │  Storage        │         │  Database        │          │  │
│  │  │  (volumes)      │         │  (volumes)       │          │  │
│  │  └─────────────────┘         └──────────────────┘          │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────┐                                              │
│  │  Registry UI     │  (Optional)                                 │
│  │  :5001           │                                              │
│  └──────────────────┘                                              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ▲
                              │ SSH Tunnel
                              │ localhost:5000
                              │
┌─────────────────────────────┴───────────────────────────────────────┐
│                      Developer Machine                              │
│                                                                     │
│  ┌──────────────┐      ┌──────────────┐      ┌─────────────────┐  │
│  │   Source     │──►   │   Docker     │──►   │  Push to        │  │
│  │   Code       │build │   Image      │push  │  Registry       │  │
│  └──────────────┘      └──────────────┘      └─────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 Процесс развертывания

### 1. Разработка и сборка (Локальная машина)

```
┌──────────────────────────────────────────────────────────────┐
│  Developer Machine                                           │
│                                                              │
│  1. Изменения в коде                                         │
│     ├── src/                                                 │
│     ├── tests/                                               │
│     └── Dockerfile                                           │
│                                                              │
│  2. Запуск тестов                                            │
│     └── dotnet test                                          │
│                                                              │
│  3. Сборка Docker образа                                     │
│     └── docker build -t localhost:5000/app:latest .          │
│                                                              │
│  4. Отправка в Registry                                      │
│     └── docker push localhost:5000/app:latest                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
                           │
                           │ SSH Tunnel (port 5000)
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  VPS: Private Registry                                       │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Docker Registry                                       │ │
│  │  - Хранит образы                                       │ │
│  │  - Версионирование (tags)                              │ │
│  │  - Аутентификация                                      │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 2. Деплой на VPS

```
┌──────────────────────────────────────────────────────────────┐
│  VPS: Application Deployment                                 │
│                                                              │
│  1. Загрузка образа из Registry                              │
│     └── docker pull localhost:5000/app:latest                │
│                                                              │
│  2. Остановка старого контейнера                             │
│     └── docker compose stop app                              │
│                                                              │
│  3. Запуск нового контейнера                                 │
│     └── docker compose up -d app                             │
│                                                              │
│  4. Проверка здоровья                                        │
│     └── docker compose ps                                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 📦 Структура образов в Registry

```
localhost:5000/family-task-manager
├── latest              # Последняя версия
├── main                # Ветка main
├── develop             # Ветка develop
├── abc123              # Commit hash
├── v1.0.0              # Версия релиза
└── 2024-11-23          # Дата сборки
```

## 🔐 Безопасность и сетевая изоляция

```
┌─────────────────────────────────────────────────────────────────┐
│                         Internet                                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ SSH (port 22)
                             │ HTTPS (port 443) - optional
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      VPS Firewall                               │
│  ✅ Allow: SSH (22) from specific IPs                           │
│  ✅ Allow: HTTPS (443) - if web interface                       │
│  ❌ Block: Registry (5000) - internal only                      │
│  ❌ Block: Registry UI (5001) - internal only                   │
│  ❌ Block: PostgreSQL (5432) - internal only                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    Docker Network (Internal)                    │
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌─────────────────┐  │
│  │  Registry    │◄───│  App         │◄───│  PostgreSQL     │  │
│  │  :5000       │    │  :8080       │    │  :5432          │  │
│  └──────────────┘    └──────────────┘    └─────────────────┘  │
│                                                                 │
│  Все сервисы изолированы от внешнего мира                      │
│  Доступны только через Docker network                          │
└─────────────────────────────────────────────────────────────────┘
```

## 🚀 CI/CD Pipeline с Private Registry

```
┌──────────────────────────────────────────────────────────────────┐
│  GitHub Repository                                               │
│                                                                  │
│  Developer pushes code                                           │
│  └── git push origin main                                        │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             │ Webhook trigger
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│  GitHub Actions                                                  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Job 1: Test                                               │ │
│  │  ├── Checkout code                                         │ │
│  │  ├── Run unit tests                                        │ │
│  │  ├── Run integration tests                                 │ │
│  │  └── Generate coverage report                              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                             │                                    │
│                             │ If tests pass                      │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Job 2: Build & Push                                       │ │
│  │  ├── Setup SSH tunnel to VPS                               │ │
│  │  ├── Login to Private Registry                             │ │
│  │  ├── Build Docker image                                    │ │
│  │  └── Push to Registry                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                             │                                    │
│                             │ If push successful                 │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Job 3: Deploy                                             │ │
│  │  ├── SSH to VPS                                            │ │
│  │  ├── Pull new image from Registry                          │ │
│  │  ├── Run deploy script                                     │ │
│  │  └── Verify deployment                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                             │
                             │ Deployment complete
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│  VPS: Running Application                                        │
│  ✅ New version deployed                                         │
│  ✅ Old version backed up                                        │
│  ✅ Database migrated                                            │
└──────────────────────────────────────────────────────────────────┘
```

## 💾 Хранение данных

```
VPS File System
│
├── /opt/docker-registry/
│   ├── docker-compose.registry.yml
│   ├── registry-auth/
│   │   └── htpasswd                    # Пользователи registry
│   └── registry-data/                  # Docker volumes
│       └── docker/
│           └── registry/
│               └── v2/
│                   └── repositories/
│                       └── family-task-manager/
│                           ├── _manifests/
│                           └── _layers/
│
├── /opt/family-task-manager/
│   ├── docker-compose.prod.yml
│   ├── .env                            # Конфигурация
│   ├── scripts/
│   │   ├── deploy-from-registry.sh
│   │   └── init-db.sql
│   └── volumes/                        # Docker volumes
│       └── postgres_data/              # База данных
│
└── /opt/backups/
    └── family-task-manager/
        ├── db_backup_20241123_120000.sql
        ├── db_backup_20241123_150000.sql
        └── ...
```

## 🔄 Жизненный цикл образа

```
┌─────────────────────────────────────────────────────────────────┐
│  1. Development                                                 │
│     └── Код изменяется, тесты пишутся                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. Build                                                       │
│     └── docker build -t localhost:5000/app:abc123 .             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. Push to Registry                                            │
│     ├── docker push localhost:5000/app:abc123                   │
│     ├── docker push localhost:5000/app:latest                   │
│     └── docker push localhost:5000/app:main                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. Storage in Registry                                         │
│     └── Образ хранится с несколькими тегами                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. Pull & Deploy                                               │
│     └── docker pull localhost:5000/app:latest                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  6. Running in Production                                       │
│     └── Контейнер работает на VPS                               │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  7. Monitoring & Logs                                           │
│     ├── docker logs -f app                                      │
│     └── Мониторинг метрик                                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  8. Update or Rollback                                          │
│     ├── Новая версия: pull latest                              │
│     └── Откат: pull previous tag                               │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 Сравнение с альтернативами

### Private Registry vs Build на VPS

```
Private Registry:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  Build       │───►│  Registry    │───►│  Deploy      │
│  (Local/CI)  │    │  (VPS)       │    │  (VPS)       │
│  ~2-3 min    │    │  Storage     │    │  ~30 sec     │
└──────────────┘    └──────────────┘    └──────────────┘
✅ Быстрый деплой
✅ Версионирование
⚠️ Требует registry

Build на VPS:
┌──────────────┐    ┌──────────────────────────────────┐
│  Git Push    │───►│  Build + Deploy (VPS)            │
│  (Local)     │    │  ~3-5 min                        │
└──────────────┘    └──────────────────────────────────┘
✅ Простая настройка
⚠️ Медленный деплой
⚠️ Требует ресурсов
```

## 📊 Метрики и мониторинг

```
┌─────────────────────────────────────────────────────────────────┐
│  Мониторинг системы                                             │
│                                                                 │
│  Registry:                                                      │
│  ├── Размер хранилища: du -sh /opt/docker-registry/            │
│  ├── Количество образов: curl .../v2/_catalog                   │
│  └── Статус: docker compose ps                                  │
│                                                                 │
│  Application:                                                   │
│  ├── CPU/RAM: docker stats                                      │
│  ├── Логи: docker logs -f app                                   │
│  ├── Healthcheck: docker inspect app                            │
│  └── Uptime: docker ps                                          │
│                                                                 │
│  Database:                                                      │
│  ├── Размер БД: SELECT pg_database_size(...)                   │
│  ├── Активные подключения: SELECT count(*) FROM pg_stat_...    │
│  └── Производительность: pg_stat_statements                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🛠️ Инструменты управления

```
Локальная машина:
├── build-and-push.ps1       # Сборка и отправка
├── SSH tunnel               # Безопасный доступ к registry
└── docker login             # Аутентификация

VPS:
├── setup-registry.sh        # Первоначальная настройка
├── deploy-from-registry.sh  # Деплой приложения
├── Registry UI (5001)       # Веб-интерфейс для просмотра
└── Docker commands          # Управление контейнерами
```

---

## 📚 Дополнительные ресурсы

- [Полная документация](PRIVATE_REGISTRY_SETUP.md)
- [Быстрый старт](../QUICK_DEPLOY_REGISTRY.md)
- [Сравнение вариантов](DEPLOYMENT_OPTIONS.md)
- [Чек-лист деплоя](../.github/DEPLOYMENT_CHECKLIST.md)

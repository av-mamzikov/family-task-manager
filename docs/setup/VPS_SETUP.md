# 🖥️ Настройка VPS для деплоя

Единый скрипт для полной настройки VPS за 5 минут.

## Требования

- VPS с Ubuntu 20.04+ (минимум 1GB RAM, 1 CPU)
- SSH доступ с правами root

## Подготовка (на вашем компьютере)

### 1. Сгенерируйте SSH ключи

#### Ключ для вашего доступа (администратор)

```powershell
# Windows
ssh-keygen -t ed25519 -f $HOME\.ssh\deploy_key -C "admin"

# Linux/macOS
ssh-keygen -t ed25519 -f ~/.ssh/deploy_key -C "admin"
```

#### Ключ для GitHub Actions

```powershell
# Windows
ssh-keygen -t ed25519 -f $HOME\.ssh\github_actions_key -C "github-actions"

# Linux/macOS
ssh-keygen -t ed25519 -f ~/.ssh/github_actions_key -C "github-actions"
```

### 2. Скопируйте публичные ключи

```powershell
# Windows - скопируйте содержимое файлов
Get-Content $HOME\.ssh\deploy_key.pub
Get-Content $HOME\.ssh\github_actions_key.pub

# Linux/macOS
cat ~/.ssh/deploy_key.pub
cat ~/.ssh/github_actions_key.pub
```

Сохраните эти ключи - они понадобятся при запуске скрипта.

## Настройка VPS (один скрипт)

### 1. Скачайте и запустите скрипт инициализации

```bash
# Скачайте скрипт
curl -o init-vps.sh https://raw.githubusercontent.com/ваш_username/family-task-manager/main/scripts/init-vps.sh

# Или скопируйте с локальной машины
scp scripts/init-vps.sh root@ваш_ip:/root/

# Запустите скрипт
bash init-vps.sh
```

### 2. Запустить скрипт

```bash
# Подключитесь к ssh
ssh root@ваш_ip_адрес

# Запустите скрипт
bash init-vps.sh
```

### 3. Следуйте инструкциям скрипта

Скрипт запросит:

1. **SSH ключ администратора** - вставьте содержимое `deploy_key.pub`
2. **SSH ключ GitHub Actions** - вставьте содержимое `github_actions_key.pub`
3. **Имя пользователя для Docker Registry** - например, `admin`
4. **Пароль для Docker Registry** - придумайте сильный пароль
5. **Имя пользователя БД** - по умолчанию `familytask`
6. **Пароль для БД** - придумайте сильный пароль
7. **Telegram Bot Token** (опционально) - можно оставить пустым
8. **Telegram Bot Username** (опционально) - можно оставить пустым

### Что установит скрипт:

- ✅ Docker и Docker Compose
- ✅ Базовые утилиты (curl, git, apache2-utils)
- ✅ Пользователь `deploy` с sudo-правами и доступом к Docker
- ✅ SSH ключи для администратора и GitHub Actions
- ✅ Директории `/opt/family-task-manager` и `/opt/docker-registry`
- ✅ Private Docker Registry с аутентификацией (подробнее: [Registry Setup](../PRIVATE_REGISTRY_SETUP.md))
- ✅ Registry UI для просмотра образов
- ✅ Файл `.env` с настройками
- ✅ UFW Firewall (опционально)

## После завершения скрипта

### 1. Проверьте подключение

```bash
# Windows
ssh -i $HOME\.ssh\deploy_key deploy@ваш_ip

# Linux/macOS
ssh -i ~/.ssh/deploy_key deploy@ваш_ip
```

> 🎉 Теперь работайте от пользователя `deploy`, а не от root!

### 2. (Опционально) Настройте SSH config для удобства

Создайте/отредактируйте `~/.ssh/config`:

```
Host myvps
    HostName ваш_ip
    User deploy
    IdentityFile ~/.ssh/deploy_key

Host myvps-root
    HostName ваш_ip
    User root
    IdentityFile ~/.ssh/id_ed25519
```

Теперь можно подключаться просто:
```bash
ssh myvps
```

## Настройка GitHub Secrets

После завершения скрипта вы получите все необходимые данные. Скопируйте их и добавьте в GitHub.

### Получение приватного ключа GitHub Actions

```powershell
# Windows
Get-Content $HOME\.ssh\github_actions_key

# Linux/macOS
cat ~/.ssh/github_actions_key
```

Скопируйте **весь вывод** (включая `-----BEGIN` и `-----END`).

### Добавление секретов в GitHub

> 📖 **Подробная инструкция:** [Secrets Setup Guide](SECRETS_SETUP.md)

Перейдите в репозиторий: `Settings` → `Secrets and variables` → `Actions` → `New repository secret`

Вам потребуется добавить **9 обязательных секретов**. Все необходимые данные скрипт `init-vps.sh` выводит в конце
работы.

Список секретов и подробные инструкции см. в [Secrets Setup Guide](SECRETS_SETUP.md).

### Docker Hub (опционально, но рекомендуется)

Для избежания лимитов на скачивание образов (Docker Hub Rate Limit) рекомендуется добавить авторизацию:

| Секрет               | Описание                                         |
|----------------------|--------------------------------------------------|
| `DOCKERHUB_USERNAME` | Ваш username на Docker Hub                       |
| `DOCKERHUB_PASSWORD` | Access Token от Docker Hub (см. инструкцию ниже) |

**Как получить Docker Hub Access Token:**

1. **Зарегистрируйтесь на Docker Hub** (если нет аккаунта):
    - Перейдите на https://hub.docker.com/signup
    - Можно использовать вход через Google

2. **Узнайте свой username**:
    - Зайдите на https://hub.docker.com
    - Нажмите на аватар в правом верхнем углу
    - Ваш username показан там (например, `johndoe123`)

3. **Создайте Access Token**:
    - Нажмите на аватар → **Account Settings**
    - Слева выберите **Security**
    - Найдите раздел **Access Tokens**
    - Нажмите **New Access Token**
    - Заполните:
        - **Description**: `GitHub Actions` (или любое название)
        - **Access permissions**: выберите **Read-only** (достаточно для pull)
    - Нажмите **Generate**
    - **ВАЖНО**: Скопируйте токен сразу! Он больше не покажется

4. **Добавьте в GitHub Secrets**:
   ```
   DOCKERHUB_USERNAME = ваш username с Docker Hub
   DOCKERHUB_PASSWORD = сгенерированный Access Token (длинная строка вида dckr_pat_...)
   ```

**Зачем это нужно:**

- Без авторизации: **100 pulls за 6 часов** с одного IP
- С авторизацией: **200 pulls за 6 часов**
- Это помогает избежать ошибок при деплое, когда Docker пытается скачать образы (например, `postgres:16-alpine`)

**Проверка на VPS:**

После настройки секретов, можете проверить авторизацию на VPS:

```bash
ssh deploy@ваш_ip

# Авторизуйтесь в Docker Hub
docker login -u ваш_username

# Введите Access Token вместо пароля

# Проверьте лимиты
TOKEN=$(curl -s "https://auth.docker.io/token?service=registry.docker.io&scope=repository:ratelimitpreview/test:pull" | jq -r .token)
curl -s --head -H "Authorization: Bearer $TOKEN" https://registry-1.docker.io/v2/ratelimitpreview/test/manifests/latest | grep -i ratelimit
```

### Для PR Preview (опционально)

Если планируете использовать PR Preview окружения, добавьте еще 4 секрета.

Подробнее см. в [Secrets Setup Guide](SECRETS_SETUP.md#опциональные-для-pr-preview-4-штуки).

## Первый деплой

Всё готово! Теперь просто запушьте код:

```bash
git add .
git commit -m "Setup deployment"
git push origin main
```

### Что произойдёт автоматически

1. ✅ Запустятся тесты
2. ✅ Соберётся Docker образ
3. ✅ Образ загрузится в registry на VPS
4. ✅ `docker-compose.prod.yml` скопируется на VPS и переименуется в `docker-compose.yml`
5. ✅ Приложение задеплоится и запустится
6. ✅ EF Core создаст схему БД автоматически

> 💡 **Примечание:** На VPS файл называется `docker-compose.yml` для упрощения работы. Вам не нужно указывать
`-f docker-compose.prod.yml` при запуске команд.

### Проверка деплоя

```bash
# Подключитесь к VPS
ssh deploy@ваш_ip

# Проверьте статус контейнеров
cd /opt/family-task-manager
docker compose ps

# Просмотр логов
docker compose logs -f
```

### Доступ к Registry UI

Registry UI доступен только через SSH туннель (порт привязан к localhost):

```bash
# Создайте SSH туннель
ssh -L 5001:localhost:5001 deploy@ваш_ip

# Откройте в браузере
http://localhost:5001
```

> 📖 Подробнее о работе с Registry: [Private Registry Setup](../PRIVATE_REGISTRY_SETUP.md)

## Troubleshooting

> 📖 **Подробное руководство:** См. соответствующие разделы в:
> - [Private Registry Setup](../PRIVATE_REGISTRY_SETUP.md#troubleshooting) - проблемы с Registry
> - [Secrets Setup](SECRETS_SETUP.md#troubleshooting) - проблемы с GitHub Actions

### Быстрые решения

### Контейнеры приложения не запускаются

```bash
ssh deploy@ваш_ip

# Проверьте логи
cd /opt/family-task-manager
docker compose logs

# Проверьте .env файл
cat .env

# Проверьте доступность БД
docker compose exec postgres pg_isready
```

### GitHub Actions не может подключиться к VPS

```bash
ssh deploy@ваш_ip

# Проверьте SSH ключи
cat ~/.ssh/authorized_keys

# Должны быть 2 ключа: ваш и GitHub Actions

# Проверьте права
ls -la ~/.ssh/
# Должно быть:
# drwx------ .ssh
# -rw------- authorized_keys

# Если права неправильные:
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### Не могу подключиться от пользователя deploy

```bash
# Подключитесь как root
ssh root@ваш_ip

# Проверьте, что пользователь создан
id deploy

# Проверьте SSH ключи
cat /home/deploy/.ssh/authorized_keys

# Если ключей нет, добавьте вручную:
echo "ваш_публичный_ключ" >> /home/deploy/.ssh/authorized_keys
chown deploy:deploy /home/deploy/.ssh/authorized_keys
chmod 600 /home/deploy/.ssh/authorized_keys
```


## Полезные команды

### SSH туннели для доступа к UI

Registry UI доступен только через SSH туннели для безопасности:

```bash
# Registry UI (просмотр Docker образов)
ssh -L 5001:localhost:5001 deploy@ваш_ip
# Откройте: http://localhost:5001
```

> 💡 **Совет:** Добавьте в `~/.ssh/config` для удобства:
> ```
> Host myvps
>     HostName ваш_ip
>     User deploy
>     IdentityFile ~/.ssh/deploy_key
>     LocalForward 5001 localhost:5001
>     LocalForward 5002 localhost:5002
> ```
> Теперь просто: `ssh myvps` и туннели создадутся автоматически!

### Управление контейнерами

```bash
ssh deploy@ваш_ip

# Статус всех контейнеров
docker ps -a

# Логи приложения
cd /opt/family-task-manager
docker compose logs -f family-task-manager

# Логи БД
docker compose logs -f postgres

# Перезапуск приложения
docker compose restart family-task-manager

# Остановка всех контейнеров
docker compose down

# Запуск всех контейнеров
docker compose up -d
```

### Автозапуск при перезагрузке системы

✅ **Уже настроено!** Все контейнеры используют `restart: unless-stopped`.

Это означает:

- Контейнеры автоматически запустятся после перезагрузки VPS
- Контейнеры перезапустятся при сбое
- Контейнеры НЕ запустятся, если вы их остановили вручную через `docker stop` или `docker compose down`

```bash
# Проверить политику перезапуска контейнера
docker inspect family-task-manager --format '{{.HostConfig.RestartPolicy.Name}}'
# Должно быть: unless-stopped

# Проверить все контейнеры
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.State}}"

# Тест: перезагрузите VPS
sudo reboot

# После перезагрузки проверьте, что контейнеры запустились
ssh deploy@ваш_ip
docker ps
# Все контейнеры должны быть в статусе "Up"
```

**Какие контейнеры настроены на автозапуск:**

- ✅ `family-task-manager` - основное приложение
- ✅ `family-task-postgres` - база данных
- ✅ `docker-registry` - приватный registry
- ✅ `docker-registry-ui` - UI для registry

**Политики перезапуска Docker:**

| Политика         | Описание                       | Когда использовать                  |
|------------------|--------------------------------|-------------------------------------|
| `no`             | Не перезапускать               | Временные контейнеры                |
| `always`         | Всегда перезапускать           | Критичные сервисы                   |
| `on-failure`     | Только при ошибке              | Для отладки                         |
| `unless-stopped` | Всегда, кроме ручной остановки | **Production (используется у нас)** |

> 💡 **Совет:** `unless-stopped` - лучший выбор для production, т.к. контейнеры не запустятся после
`docker compose down`, что удобно при обслуживании.

### Управление Registry

> 📖 Подробные команды см. в:
> - [Registry Commands Cheatsheet](../REGISTRY_COMMANDS_CHEATSHEET.md)

### Резервное копирование БД

```bash
# Создание бэкапа
cd /opt/family-task-manager
docker compose exec postgres \
  pg_dump -U familytask familytask > backup_$(date +%Y%m%d).sql

# Восстановление из бэкапа
cat backup_20241124.sql | docker compose exec -T postgres \
  psql -U familytask familytask
```

## Следующие шаги

- 📖 [GitHub Actions Setup](GITHUB_ACTIONS_SETUP.md) - настройка CI/CD
- 🚀 [Deployment Summary](../../DEPLOYMENT_SUMMARY.md) - обзор процесса деплоя
- 🐳 [Docker Registry Setup](../PRIVATE_REGISTRY_SETUP.md) - подробнее о registry

---

**Готово!** Ваш VPS настроен и готов к автоматическому деплою. 🎉

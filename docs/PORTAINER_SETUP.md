# 🐳 Portainer - Web UI для управления Docker

Portainer предоставляет удобный веб-интерфейс для управления Docker контейнерами на VPS.

> 🚨 **Пропустили 5 минут при первом входе?** Выполните: `cd /opt/portainer && docker compose restart` и быстро создайте
> администратора!

## 🚀 Быстрая установка

### На VPS

```bash
# Создайте директорию
mkdir -p /opt/portainer
cd /opt/portainer

# Создайте docker-compose.yml
cat > docker-compose.yml << 'EOF'
version: '3.8'

services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    networks:
      - portainer-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  portainer_data:
    driver: local

networks:
  portainer-network:
    driver: bridge
EOF

# Запустите Portainer
docker compose up -d
```

### Или скопируйте из репозитория

```bash
# На вашем компьютере
scp docker-compose.portainer.yml root@ваш_ip:/opt/portainer/docker-compose.yml

# На VPS
ssh root@ваш_ip
cd /opt/portainer
docker compose up -d
```

## 🌐 Доступ

Откройте в браузере:

- **HTTP:** `http://ваш_ip:9000`
- **HTTPS:** `https://ваш_ip:9443`

### Первый вход

При первом входе создайте admin аккаунт.

> ⚠️ **Важно:** У вас есть **5 минут** после первого запуска Portainer для создания администратора!

### ❗ Пропустили 5 минут? Portainer заблокирован?

Если вы видите сообщение о блокировке или не успели создать администратора за 5 минут:

```bash
# Подключитесь к VPS
ssh deploy@ваш_ip

# Перейдите в директорию Portainer
cd /opt/portainer

# Перезапустите контейнер (сбросит таймер)
docker compose restart

# Или пересоздайте контейнер
docker compose down
docker compose up -d
```

После перезапуска у вас снова будет 5 минут для создания администратора. Откройте браузер и сразу перейдите на
`http://ваш_ip:9000`.

## ✨ Основные возможности

### 📊 Dashboard

- Обзор всех контейнеров
- Статистика использования ресурсов
- Быстрый доступ к управлению

### 🐳 Управление контейнерами

- **Start/Stop/Restart** - управление состоянием
- **Logs** - просмотр логов в реальном времени
- **Stats** - мониторинг CPU, RAM, Network, Disk I/O
- **Exec** - выполнение команд внутри контейнера через браузер
- **Inspect** - детальная информация о контейнере

### 📦 Управление образами

- Список всех образов
- Pull новых образов
- Удаление неиспользуемых образов
- Просмотр истории слоёв

### 💾 Volumes и Networks

- Управление Docker volumes
- Создание и удаление networks
- Просмотр использования

### 📚 Stacks (Docker Compose)

- Деплой через docker-compose.yml
- Редактирование compose файлов в UI
- Управление несколькими стеками

## 🔐 Безопасность

### Настройка firewall

Если хотите ограничить доступ к Portainer:

```bash
# Разрешить доступ только с вашего IP
sudo ufw allow from ваш_ip to any port 9000
sudo ufw allow from ваш_ip to any port 9443

# Или используйте SSH туннель
ssh -L 9000:localhost:9000 root@ваш_vps_ip
# Теперь доступ через http://localhost:9000
```

### HTTPS с Let's Encrypt

Portainer поддерживает автоматическое получение SSL сертификатов:

```bash
docker run -d \
  -p 9000:9000 \
  -p 9443:9443 \
  --name portainer \
  --restart=always \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v portainer_data:/data \
  portainer/portainer-ce:latest \
  --sslcert /path/to/cert.pem \
  --sslkey /path/to/key.pem
```

## 📖 Полезные команды

```bash
# Проверить статус Portainer
docker ps | grep portainer

# Просмотр логов Portainer
docker logs portainer -f

# Перезапуск Portainer
docker restart portainer

# Обновление Portainer
cd /opt/portainer
docker compose pull
docker compose up -d

# Удаление Portainer
docker compose down
docker volume rm portainer_data
```

## 🎯 Типичные задачи

### Просмотр логов приложения

1. Откройте Portainer
2. Перейдите в **Containers**
3. Найдите `family-task-manager`
4. Нажмите на имя контейнера
5. Выберите **Logs**
6. Включите **Auto-refresh** для реального времени

### Перезапуск контейнера

1. **Containers** → найдите контейнер
2. Нажмите **Quick actions** → **Restart**

### Выполнение команды в контейнере

1. **Containers** → выберите контейнер
2. **Console** → выберите `/bin/sh` или `/bin/bash`
3. Нажмите **Connect**

### Мониторинг ресурсов

1. **Containers** → выберите контейнер
2. **Stats** → графики CPU, RAM, Network, I/O

## 🔧 Troubleshooting

### Portainer заблокирован (пропустили 5 минут)

**Проблема:** При открытии Portainer показывает сообщение о блокировке или таймауте.

**Решение:**

```bash
# Подключитесь к VPS
ssh deploy@ваш_ip

# Перезапустите контейнер
cd /opt/portainer
docker compose restart

# Быстро откройте браузер и создайте администратора
# У вас есть 5 минут!
```

### Забыли пароль администратора

**Решение:** Сброс данных Portainer

```bash
ssh deploy@ваш_ip
cd /opt/portainer

# Остановите и удалите данные
docker compose down
docker volume rm portainer_portainer_data

# Запустите заново
docker compose up -d

# Создайте нового администратора
```

### Portainer не запускается

**Проверка:**

```bash
# Проверьте статус
docker ps -a | grep portainer

# Проверьте логи
docker logs portainer

# Проверьте, не занят ли порт
sudo netstat -tulpn | grep 9000
```

**Решение:**

```bash
cd /opt/portainer
docker compose down
docker compose up -d
```

### "The environment named local is unreachable"

**Проблема:** Portainer не может подключиться к Docker daemon.

**Причины и решения:**

#### 1. Docker socket недоступен

```bash
# Проверьте, существует ли socket
ls -la /var/run/docker.sock

# Должно быть: srw-rw---- 1 root docker ... /var/run/docker.sock

# Если файла нет, перезапустите Docker
sudo systemctl restart docker

# Проверьте, что Docker работает
sudo systemctl status docker
```

#### 2. Проблема с правами доступа ⭐ (ЧАСТАЯ ПРИЧИНА)

**Как это работает:**

- Portainer работает внутри Docker контейнера от `root`
- Контейнеру монтируется `/var/run/docker.sock` с хоста
- Docker daemon проверяет права доступа к socket на основе пользователя, запустившего контейнер
- Socket имеет права `srw-rw---- 1 root docker` - доступ только для группы `docker`

**Симптомы:**

- Portainer показывает Docker как доступный
- При клике статус меняется на "Down"
- Ошибка: "Failed loading environment. The environment named local is unreachable"
- Через терминал Docker работает нормально

```bash
# Проверьте права на socket
ls -la /var/run/docker.sock
# Должно быть: srw-rw---- 1 root docker ...

# Проверьте, от какого пользователя вы работаете
whoami
groups
# Должна быть группа "docker" в списке!

# Проверьте, кто запустил контейнер
ps aux | grep portainer

# Если группы docker нет, добавьте пользователя
sudo usermod -aG docker $USER

# ВАЖНО: После добавления нужно выйти и зайти заново!
exit
# Затем снова подключитесь по SSH

# Или обновите группы без выхода
newgrp docker

# Проверьте, что группа применилась
groups
# Теперь должна быть "docker"

# КРИТИЧНО: Пересоздайте Portainer от пользователя с группой docker!
cd /opt/portainer
docker compose down
docker compose up -d

# Проверьте, что контейнер запустился
docker ps | grep portainer

# Проверьте монтирование socket
docker inspect portainer --format '{{json .Mounts}}' | grep docker.sock

# Проверьте логи Portainer на ошибки
docker logs portainer --tail 50
```

**Дополнительная проверка - права внутри контейнера:**

```bash
# Проверьте, может ли контейнер читать socket
docker exec portainer ls -la /var/run/docker.sock

# Попробуйте выполнить команду Docker из контейнера
docker exec portainer docker ps
# Если ошибка "permission denied" - проблема с правами!
```

**Альтернативное решение - запуск с GID группы docker:**

Если пересоздание контейнера не помогло, можно явно указать GID группы docker:

```bash
# Узнайте GID группы docker
getent group docker
# Вывод: docker:x:999:deploy (999 - это GID)

# Или так
grep docker /etc/group
# docker:x:999:deploy

# Запомните число (например, 999)

# Остановите Portainer
cd /opt/portainer
docker compose down

# Отредактируйте docker-compose.yml
nano docker-compose.yml

# Добавьте в секцию portainer:
#   user: "0:999"  # 0 - root, 999 - GID группы docker
# Или добавьте group_add:
#   group_add:
#     - "999"  # GID группы docker

# Пример полной конфигурации:
cat > docker-compose.yml << 'EOF'
services:
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    group_add:
      - "999"  # Замените на ваш GID группы docker
    networks:
      - portainer-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  portainer_data:
    driver: local

networks:
  portainer-network:
    driver: bridge
EOF

# Запустите заново
docker compose up -d

# Проверьте
docker logs portainer --tail 20
```

#### 3. Контейнер запущен без доступа к socket

```bash
# Проверьте монтирование в запущенном контейнере
docker inspect portainer | grep -A 5 Mounts

# Должен быть: /var/run/docker.sock:/var/run/docker.sock

# Если нет, пересоздайте контейнер
cd /opt/portainer
docker compose down
docker compose up -d
```

#### 4. SELinux блокирует доступ (для CentOS/RHEL)

```bash
# Проверьте SELinux
getenforce

# Если Enforcing, временно отключите для теста
sudo setenforce 0

# Если помогло, добавьте правило
sudo chcon -Rt svirt_sandbox_file_t /var/run/docker.sock

# Или отключите SELinux постоянно (не рекомендуется)
sudo vi /etc/selinux/config
# SELINUX=disabled
```

#### 5. Быстрое решение - пересоздание

```bash
# Полное пересоздание Portainer
cd /opt/portainer
docker compose down
docker rm -f portainer 2>/dev/null
docker volume prune -f
docker compose up -d

# Проверьте логи
docker logs portainer -f
```

**После исправления:**

1. Откройте Portainer: `http://ваш_ip:9000`
2. Создайте администратора
3. Выберите "Get Started" - должно подключиться автоматически

## 🔄 Альтернативы

Если Portainer не подходит:

- **Dockge** - легковесная альтернатива для docker-compose
- **Lazydocker** - TUI в терминале
- **Yacht** - минималистичный UI
- **Cockpit** - системный мониторинг + Docker плагин

## 📚 Дополнительные ресурсы

- [Официальная документация](https://docs.portainer.io/)
- [GitHub](https://github.com/portainer/portainer)
- [Community Edition vs Business](https://www.portainer.io/pricing)

---

**Совет:** Portainer отлично подходит для начинающих и для быстрого управления контейнерами. Для production
рекомендуется также настроить мониторинг (Prometheus + Grafana) и логирование (ELK/Loki).

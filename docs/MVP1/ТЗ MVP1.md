# ТЗ — MVP «Семейный менеджер дел» (Telegram-бот)

## 1. Цель продукта и описание MVP

Минимальный продукт для семей, позволяющий:

* вести задачи по дому (разовые, периодические);
* мотивировать детей через питомца, очки и лидерборд;
* вести семейную статистику и историю действий;
* использовать Telegram как единственный интерфейс;
* геймифицировать выполнение задач для создания мотивации.

## 2. Основной функционал (Functional Requirements)

### 2.1. Управление семьёй

**Регистрация и создание:**
* При первом `/start` создаётся запись `User` с привязкой по `telegramId`.
* Пользователь может создать новую семью или присоединиться к существующей по invite_code.
* Один Telegram-пользователь может быть участником нескольких семей.
* Создатель семьи автоматически получает роль **Админ**.

**Приглашения:**
* Формат: deep link `t.me/bot_name?start=invite_FAMILY_ID_ROLE`.
* Invite code генерируется для конкретной семьи и роли (например, `invite_123_child`).
* Код действителен бессрочно, многоразовый (пока не удалён Админом).
* При переходе по ссылке пользователь автоматически добавляется в семью с указанной ролью.
* Фактическое значение `invite_code` представляет собой непрозрачный токен (opaque identifier), не содержащий реального `familyId`; соответствие токена и семьи хранится в отдельной таблице приглашений.

**Роли в семье:**
* Роли (фиксированное перечисление): **Админ**, **Взрослый**, **Ребёнок**.
* Роль назначается в контексте конкретной семьи (один пользователь может быть Админом в одной семье и Ребёнком в другой).
* Роль хранится в связке `FamilyMember` (userId + familyId + role + points).

**Текущая активная семья:**
* Пользователь выбирает активную семью через команду `/family`.
* Выбранная семья сохраняется в In-Memory сессии (`currentFamilyId`).
* Все команды (`/tasks`, `/pet`, `/stats`) работают в контексте текущей активной семьи.
* При первом входе активной становится первая семья пользователя.

**Настройки семьи:**
* Включение/отключение лидерборда.
* Timezone семьи (для расписания задач), по умолчанию UTC.

**Права Админа:**
* Создание/удаление семьи.
* Приглашение/удаление участников.
* Назначение/изменение ролей участников.
* Создание/редактирование/удаление задач.
* Создание/удаление питомцев.
* Изменение настроек семьи.

Удаление семьи и сущностей:
* Удаление семьи выполняется как soft delete: данные в БД (задачи, история, питомцы, участники) сохраняются, но семья недоступна в интерфейсе.
* Удаление питомца возможно только при отсутствии активных задач и шаблонов, связанных с этим питомцем, иначе операция блокируется.
* Удаление задач выполняется как деактивация шаблона/экземпляров без удаления истории (`ActionHistory`).

**Удаление участника:**
* История выполненных задач сохраняется (с пометкой "удалённый участник").
* Очки участника остаются в истории, но не учитываются в текущем лидерборде.

### 2.2. Питомцы

**Общие параметры:**
* В семье может быть несколько питомцев.
* Тип питомца (кот, собака, хомяк) влияет только на тексты/рендер.
* Параметры: настроение (числовое значение 0-100).
* Создание питомца доступно только Админу.
* Для каждого типа питомца в системе определён базовый набор рекомендованных периодических задач (глобальные шаблоны), задаваемый в коде (конфигурация/seed-данные).
* При создании питомца для конкретной семьи на основе его типа автоматически создаются семейные `TaskTemplate` по этому набору и привязываются к питомцу.

**Редактирование и удаление питомцев:**
* Админ может изменить имя питомца.
* Изменение типа питомца для уже созданного питомца в MVP1 не поддерживается (для смены типа необходимо удалить питомца и создать нового).
* Удаление питомца возможно только при отсутствии активных `TaskTemplate` и `TaskInstance`, связанных с этим питомцем; в противном случае операция блокируется.

**Механика настроения:**
* Отображаемое настроение питомца вычисляется **на лету** по всем связанным задачам (`TaskInstance`) с учётом их очков и просроченности.
* Для каждой задачи, привязанной к питомцу, задаётся срок выполнения (`dueAt`), который определяется расписанием для периодических задач или явным дедлайном для разовых задач.
* В расчёт попадают только задачи, у которых срок уже наступил: `dueAt <= now()`.
* Для каждой такой задачи вычисляется её фактический вклад:
  * если задача выполнена в срок (`completedAt <= dueAt`) — она даёт полный положительный вклад: `effectivePoints = points`;
  * если задача выполнена с опозданием (`completedAt > dueAt`) — она даёт частичный положительный вклад: `effectivePoints = points * k_late`, где `0 < k_late < 1` (конкретное значение настраивается в коде);
  * если задача не выполнена (`completedAt IS NULL` и `now() > dueAt`) — она даёт отрицательный вклад: `effectivePoints = - points * f(overdueTime)`, где `f(overdueTime)` — функция в диапазоне [0; 1], растущая с увеличением времени просрочки.
* Итоговое настроение вычисляется по формуле:
  * `maxPoints = sum(points) по всем задачам, срок которых уже наступил (dueAt <= now())`;
  * `effectiveSum = sum(effectivePoints) по этим задачам`;
  * `baseMood = 100 * (effectiveSum / maxPoints)` при `maxPoints > 0`, иначе `baseMood = 100`;
  * `moodScore = clamp(0, 100, round(baseMood))`.
* Поле `Pet.moodScore` хранится в БД как денормализованное значение и пересчитывается при доменных событиях, влияющих на настроение (например, `TaskCompleted`, изменение просроченности задач).
* Таким образом, если нет просроченных задач, настроение близко к 100; чем дольше важные задачи остаются невыполненными, тем сильнее они снижают настроение, но выполнение даже просроченной задачи частично восстанавливает значение.

**Привязка задач:**
* Каждая задача обязательно привязывается к конкретному питомцу (`petId` — обязательное поле).
* При выполнении задачи настроение соответствующего питомца увеличивается.

**Отображение в UI:**
* Команда `/pet` показывает список всех питомцев семьи с их текущим настроением.
* Выбор конкретного питомца показывает его задачи и детальную статистику.

### 2.3. Задачи

**Типы задач:**

* **Разовая задача:**
  * Выполняется один раз.
  * Может быть создана Админом или Взрослым.
  * Выполнить может любой участник семьи.
  * После выполнения переходит в статус "Выполнена" и остаётся в истории.
  * Хранится в БД бессрочно для статистики.

* **Периодическая задача:**
  * Повторяется по расписанию (Quartz Cron Expression).
  * Создаётся только Админом.
  * Реализована через шаблон (`TaskTemplate`) и экземпляры (`TaskInstance`):
    - `TaskTemplate`: хранит название, расписание, очки, petId.
    * `TaskInstance` (создаётся автоматически по расписанию, имеет статус, `dueAt` и дату выполнения).
  * При срабатывании расписания по `TaskTemplate` новый `TaskInstance` создаётся **только если предыдущий экземпляр, ссылающийся на этот шаблон, уже выполнен**. Если предыдущий экземпляр не выполнен и его `dueAt < now()`, новый экземпляр не создаётся, продолжает существовать один просроченный `TaskInstance`.
  * Инвариант: в каждый момент времени для одного `TaskTemplate` может существовать не более одного `TaskInstance` со статусом **Активна** (включая просроченные).
  * После выполнения экземпляр помечается "Выполнена"; при следующем срабатывании cron для этого шаблона создаётся новый экземпляр с новым `dueAt`.

**Параметры задачи:**
* `title` (название, 3-100 символов, обязательное).
* `type` (разовая/периодическая, обязательное).
* `petId` (привязка к питомцу, обязательное).
* `points` (количество очков за выполнение, 1-100, обязательное).
* `schedule` (для периодических задач, Quartz Cron Expression, опциональное).
* `roomId`, `projectId` (опциональные, Phase 2).

**Редактирование задач:**
* Разовые задачи:
  * Админ/Взрослый может изменить название и количество очков, пока задача находится в статусе "Активна".
  * После перехода задачи в статус "Выполнена" редактирование недоступно (задача остаётся только в истории).
* Периодические задачи (`TaskTemplate`):
  * Админ может изменить название (`title`), очки (`points`) и расписание (`schedule`).
  * Изменения применяются только к будущим `TaskInstance`, уже созданные экземпляры не изменяются и остаются в истории.
  * Админ может деактивировать шаблон задачи (перевести `TaskTemplate.isActive = false`), после чего новые `TaskInstance` по нему не создаются, существующие экземпляры не удаляются.

**Примеры Quartz Cron:**
* `0 0 9 * * ?` — ежедневно в 9:00.
* `0 0 9 */5 * ?` — каждые 5 дней в 9:00.
* `0 0 20 * * ?` — ежедневно в 20:00.

Все значения cron и `dueAt` интерпретируются в часовом поясе семьи (`Family.timezone`), а в БД хранятся в UTC. При изменении `Family.timezone` новые экземпляры задач планируются уже с учётом нового пояса, существующие `TaskInstance` не пересчитываются задним числом.

**Статусы задачи (TaskInstance):**
* **Активна** — создана, доступна для взятия в работу.
* **В работе** — участник взял задачу в работу, задача остаётся в общем списке, но её нельзя взять другим участникам.
* **Выполнена** — выполнена участником (с указанием `completedBy` (userId), `completedAt` (timestamp)).

**Выполнение задачи:**
* Участник выбирает задачу из списка и нажимает кнопку "Взять в работу".
* Статус задачи меняется на "В работе", задача остаётся в списке задач, но становится недоступной для взятия другими участниками.
* После фактического выполнения участник нажимает кнопку "Выполнить":
  - Статус задачи → "Выполнена".
  - Начисляются очки участнику (в контексте семьи).
  - Увеличивается настроение питомца.
  - Отправляется уведомление всем участникам семьи.

**Валидация при создании:**
* Название: 3-100 символов.
* Очки: 1-100.
* Для периодических задач обязателен валидный Quartz Cron Expression.
* `petId` должен существовать в семье.

### 2.4. Лидерборд

**Лидерборд:**
* Отображается только если включён в настройках семьи.
* Показывает очки всех активных участников текущей семьи.
* При выключенном лидерборде очки продолжают начисляться, но общий рейтинг не отображается; участник видит только свой баланс очков в семье.
* Сортировка: по убыванию очков.
* Период: общий за всё время (в Phase 2 добавится фильтр по неделе/месяцу).
* Удалённые участники не отображаются в текущем лидерборде.

### 2.5. Очки

* Очки начисляются автоматически при выполнении задач согласно полю `points`.
* Очки хранятся в контексте семьи (в `FamilyMember.points`).
* Используются для отображения в лидерборде и личного счёта участника в семье.
* Уход в минус по очкам невозможен (минимум 0).
* Штрафов за невыполнение задач нет: баланс очков участника не может стать отрицательным, невыполненные задачи влияют только на настроение питомца (через формулу из раздела 2.2).

### 2.6. Статистика

**Метрики:**
* Очки по участникам текущей семьи.
* Количество выполненных задач (общее и по каждому участнику).
* История действий семьи (последние 100 событий).

**Фильтрация:**
* По участнику (выбор из списка).
* По периоду (за последние 7/30 дней, за всё время).
* Фильтры применяются через inline-кнопки.

**История действий включает:**
* Выполнение задач (кто, когда, какую задачу).
* Создание задач.
* Добавление участников.
* Изменения настроения питомцев.

**Хранение:**
* История хранится бессрочно в БД.
* В интерфейсе Telegram отображаются только последние 100 событий (пагинация в Phase 2).

### 2.7. Telegram-интерфейс

**Главное меню (persistent keyboard):**
* 🏠 Семья — переключение между семьями, информация о текущей семье.
* ✅ Мои задачи — список активных задач с кнопками "Выполнить".
* 🐾 Питомец — список питомцев, их настроение и задачи.
* ⭐ Мои очки — текущий баланс очков в активной семье.
* 📊 Статистика — лидерборд и история действий.
* ⚙️ Настройки — настройки семьи (только для Админа).

**Уведомления:**
* Напоминания о задачах: отправляются всем участникам семьи по расписанию Quartz (за 1 час до времени задачи).
* Выполнение задачи: уведомление всем участникам семьи о том, кто выполнил задачу.
* Изменение настроения питомца: если настроение достигло критических значений (< 20 или > 80).
* Новый участник: уведомление всем участникам о присоединении нового члена семьи.

**Частота уведомлений:**
* Напоминания о задачах: не чаще 1 раза в час для одной задачи (для каждого `TaskInstance` хранится время последней отправки напоминания, повторная отправка возможна только если прошло ≥ 1 часа).

---

## 3. Domain Model

### 3.1. Aggregates

* FamilyAggregate
* UserAggregate
* PetAggregate
* TaskAggregate

### 3.2. Entities

**Основные сущности:**

* **Family**: 
  * id (Guid), name (string), createdAt (DateTime), timezone (string, default "UTC").
  * settings: { leaderboardEnabled (bool) }.

* **User**: 
  * id (Guid), telegramId (long, unique), name (string), createdAt (DateTime).
  * name заполняется из Telegram (first_name + last_name или username) и может обновляться при повторном `/start`.

* **FamilyMember** (связка User ↔ Family):
  * id (Guid), userId (Guid), familyId (Guid), role (enum: Admin/Adult/Child).
  * points (int, default 0), joinedAt (DateTime), isActive (bool).

* **Pet**: 
  * id (Guid), familyId (Guid), type (enum: Cat/Dog/Hamster), name (string).
  * moodScore (int, 0-100, default 50), createdAt (DateTime).
  * moodScore хранится как денормализованное значение, периодически пересчитываемое из связанных задач.

* **TaskTemplate** (для периодических задач):
  * id (Guid), familyId (Guid), petId (Guid), title (string, 3-100).
  * points (int, 1-100), schedule (string, Quartz Cron).
  * createdBy (Guid), createdAt (DateTime), isActive (bool).
  * может быть создан вручную Админом или автоматически при создании питомца на основе глобального шаблона задач для его типа.

* **TaskInstance** (экземпляры задач):
  * id (Guid), familyId (Guid), petId (Guid), title (string).
  * points (int), type (enum: OneTime/Recurring), templateId (Guid, nullable).
  * status (enum: Active/InProgress/Completed).
  * completedBy (Guid, nullable), completedAt (DateTime, nullable).
  * createdAt (DateTime).

* **Room** (Phase 2): 
  * id (Guid), familyId (Guid), name (string).

* **Project** (Phase 2): 
  * id (Guid), familyId (Guid), title (string).

* **ActionHistory** (история действий):
  * id (Guid), familyId (Guid), userId (Guid), actionType (enum).
  * description (string), metadata (JSON), createdAt (DateTime).

Примерные значения `actionType`: TaskCreated, TaskCompleted, MemberAdded, PetMoodUpdated, FamilyCreated и др. Поле `metadata` хранит JSON с деталями события (например, `{"taskId": "...", "points": 10}`).

### 3.3. Domain Events

* FamilyCreated
* MemberAdded
* TaskCreated
* TaskCompleted
* PointsCredited
* PetMoodUpdated

---

## 4. Архитектура (Clean Architecture)

Использовать Ardalis.CleanArchitecture.Template

### 4.1. Слои

* **Core**: Domain, Application (use cases).
* **Infrastructure**: Postgres (EF Core), S3/minio, Telegram Bot API adapter.
* **Bot**: Telegram Bot (обработка команд, inline-кнопок, callback queries).
* **Worker**: шедулер напоминаний для периодических задач (на основе Quartz.NET).

### 4.2. Правила

* Направление зависимостей: только наружу → внутрь запрещено.
* Домейн не зависит от инфраструктуры.

---

## 5. Telegram Bot — основные команды и сценарии

### 5.1. Команды

* `/start` — регистрация пользователя (создание User) или присоединение к семье по invite_code.
  - Формат с invite: `/start invite_123_child`.
  - Без параметров: приветствие и предложение создать семью.

* `/family` — управление семьями:
  - Список семей пользователя.
  - Переключение активной семьи.
  - Создание новой семьи.
  - Генерация invite-ссылок (только для Админа).
  - Просмотр участников.

* `/tasks` — просмотр активных задач текущей семьи (дублирует кнопку меню).

* `/pet` — просмотр питомцев текущей семьи (дублирует кнопку меню).

* `/stats` — статистика и лидерборд (дублирует кнопку меню).

* `/help` — справка по командам.

### 5.2. Inline-меню (кнопки)

Описано в разделе 2.7.

### 5.3. Сценарии взаимодействия

**Создание семьи:**
1. Пользователь нажимает "Создать семью" в `/family`.
2. Бот запрашивает название семьи.
3. Бот создаёт семью, назначает пользователя Админом.
4. Предлагает создать первого питомца.

**Приглашение участника:**
1. Админ выбирает "Пригласить участника" в `/family`.
2. Выбирает роль (Взрослый/Ребёнок).
3. Бот генерирует deep link и отправляет его Админу.
4. Админ пересылает ссылку новому участнику.
5. Новый участник переходит по ссылке, автоматически добавляется в семью.

**Редактирование семьи (название и настройки):**
1. Админ открывает раздел "Семья" или "Настройки".
2. Выбирает действие "Изменить название семьи" или "Настройки семьи".
3. Вводит новое название и/или изменяет параметры (таймзона, включение/выключение лидерборда, уведомлений).
4. Бот сохраняет изменения и обновляет отображение информации о семье.

**Создание задачи:**
1. Админ/Взрослый выбирает "Создать задачу" в настройках.
2. Выбирает тип (разовая/периодическая).
3. Вводит название, очки, выбирает питомца.
4. Для периодической: вводит Quartz Cron или выбирает из шаблонов.
5. Задача создаётся и становится доступной всем участникам.

**Редактирование периодической задачи:**
1. Админ открывает список задач семьи через `/tasks` или раздел "Мои задачи" в режиме администратора.
2. Выбирает интересующую периодическую задачу (шаблон).
3. Выбирает действие "Изменить задачу".
4. Меняет название, очки и/или cron-выражение расписания.
5. Бот сохраняет изменения в `TaskTemplate`; будущие экземпляры будут создаваться с обновлёнными параметрами, уже созданные `TaskInstance` не меняются.

**Добавление питомца:**
1. Админ открывает раздел "Питомец" или "Настройки" и выбирает "Добавить питомца".
2. Выбирает тип питомца (кот/собака/хомяк) и вводит имя.
3. Бот создаёт сущность `Pet` для текущей семьи.
4. На основе типа питомца бот создаёт семейные `TaskTemplate` по глобальному шаблону задач и привязывает их к новому питомцу.

**Редактирование питомца:**
1. Админ открывает список питомцев через `/pet`.
2. Выбирает питомца и действие "Изменить".
3. Вводит новое имя питомца.
4. Бот обновляет поле имени в сущности `Pet`.

**Выполнение задачи:**
1. Участник открывает "Мои задачи".
2. Выбирает задачу, нажимает "Взять в работу".
3. Статус задачи меняется на "В работе", задача остаётся в списке задач, но её нельзя взять другим участникам.
4. После фактического выполнения участник нажимает "Выполнить": задача помечается выполненной, начисляются очки, обновляется настроение питомца.
5. Всем участникам семьи отправляется уведомление.

---

## 6. Технические требования

### 6.1. Технологический стек

* **Язык**: C# .NET 10+.
* **БД**: PostgreSQL 15+ (EF Core 10).
* **Telegram Bot**: библиотека Telegram.Bot 19+.
* **Шедулер**: Quartz.NET 3.8+ (для периодических задач и напоминаний).
* **Контейнеризация**: Docker.
* **Dev orchestration**: .NET Aspire (упрощает запуск PostgreSQL и мониторинг в dev).
* **Логирование**: Serilog.
* **Сессии**: In-memory (ConcurrentDictionary в приложении), Redis в MVP не используется.

### 6.2. Архитектурные решения

**Модульный монолит (FamilyTaskManager.Host):**
* Единый процесс, объединяющий Bot и Worker модули.
* **Преимущества**: проще разработка, меньше overhead, общий DbContext pool.
* **Модули**:
  - **Bot Module**: Telegram Bot с Long Polling, обработка команд, сессии.
  - **Worker Module**: Quartz.NET Jobs для периодических задач и напоминаний.
* **Масштабирование**: при необходимости модули можно выделить в отдельные микросервисы.

**Структура проектов:**
```
├── FamilyTaskManager.Core          # Доменная модель (Entities, Aggregates)
├── FamilyTaskManager.UseCases      # Use Cases (Application Layer, CQRS)
├── FamilyTaskManager.Infrastructure # EF Core, PostgreSQL, Email
├── FamilyTaskManager.Host          # Модульный монолит (Bot + Worker)
│   ├── Modules/
│   │   ├── Bot/                    # Telegram Bot модуль
│   │   └── Worker/                 # Quartz Worker модуль
│   └── Program.cs                  # Единая точка входа
└── FamilyTaskManager.AspireHost    # .NET Aspire orchestration
```

**Telegram Bot API (Bot Module):**
* Режим: Long Polling (проще для MVP, не требует SSL и публичного IP).
* При масштабировании можно переключиться на Webhook.
* Persistent Menu с кнопками для основных команд.
* Inline Keyboards для интерактивных действий.

**Сессии (In-Memory):**
* Хранение в `ConcurrentDictionary<long, UserSession>` где `long` — telegramId.
* Структура: `{ currentFamilyId, conversationState, lastActivity }`.
* Очистка неактивных сессий: автоматически при неактивности > 24 часа.
* **Ограничение**: при рестарте приложения сессии теряются (пользователь просто заново выберет семью).

**S3/minio:**
* Структура хранения: `family/{familyId}/tasks/{taskInstanceId}/{photoId}.jpg`.
* Публичный доступ: нет (только через signed URLs, TTL 1 час).
* Очистка старых фото: автоматическая через lifecycle policy (хранение 1 год).

**Quartz.NET Worker (Worker Module):**
* **TaskInstanceCreatorJob**: проверка расписания каждую минуту, создание TaskInstance из TaskTemplate.
* **TaskReminderJob**: отправка напоминаний за 1 час до времени задачи (каждые 15 минут).
* **PetMoodCalculatorJob**: пересчет настроения питомцев (каждые 30 минут).
* Персистентность: Quartz использует PostgreSQL для хранения состояния (избежание дублирования при рестарте).

**Тестирование:**
* Тесты организованы по типам (Unit, Integration, Functional), а не по проектам.
* **UnitTests**: тесты Core, UseCases, Host модулей (~120 тестов).
* **IntegrationTests**: тесты репозиториев с реальной БД (~10 тестов).
* **FunctionalTests**: E2E тесты полного стека (~5 тестов).

### 6.3. Безопасность

* Валидация всех входных данных (FluentValidation).
* Защита от SQL-инъекций: параметризованные запросы (EF Core).
* Санитизация текстовых полей (защита от инъекций в сообщениях).

### 6.4. Мониторинг и эксплуатация

* **Логи**: 
  - Уровни: Information, Warning, Error.
  - Serilog с выводом в консоль и файл.

* **Бэкапы БД**: 
  - Ежедневные автоматические бэкапы PostgreSQL.
  - Хранение: 7 дней.

---

## 8. Глоссарий и примечания

### 8.1. Ключевые термины

* **Family** — семья, основная единица организации пользователей.
* **FamilyMember** — связка пользователя и семьи с ролью и очками в контексте семьи.
* **TaskTemplate** — шаблон периодической задачи с расписанием.
* **TaskInstance** — экземпляр задачи (разовой или созданной из шаблона).
* **Deep link** — ссылка формата `t.me/bot_name?start=параметр` для автоматических действий.
* **Persistent keyboard** — постоянное меню с кнопками внизу экрана Telegram.
* **Inline keyboard** — кнопки, прикреплённые к конкретному сообщению.
* **Quartz Cron Expression** — формат расписания для периодических задач.

### 8.2. Важные решения и обоснования

**Почему FamilyMember вместо роли в User?**
* Один пользователь может иметь разные роли в разных семьях.
* Очки начисляются в контексте семьи, а не глобально.

**Почему TaskTemplate и TaskInstance?**
* Периодические задачи создают новые экземпляры по расписанию.
* История выполнения хранится в TaskInstance.
* Шаблон можно изменить без потери истории.

**Почему Long Polling для MVP?**
* Проще в настройке (не требует SSL, публичного IP, настройки Webhook).
* Достаточно для низкой нагрузки (до 100-200 активных пользователей).
* При масштабировании можно переключиться на Webhook.

**Почему In-Memory сессии вместо Redis?**
* Проще для MVP (не требует отдельного сервиса).
* Достаточно для низкой нагрузки (до 100-200 активных пользователей).
* При рестарте пользователь просто заново выберет семью (не критично).
* При масштабировании можно перейти на Redis или распределённый кеш.

### 8.3. Ограничения MVP

* Максимум 100 событий в истории действий (без пагинации).
* Поддержка только русского языка (английский в Phase 2).
* Long Polling (достаточно для низкой нагрузки, до 100-200 активных пользователей).
* In-memory сессии (при рестарте приложения пользователи заново выбирают активную семью).

### 8.4. Известные технические долги

* Отсутствие пагинации в истории действий (Phase 2).
* Отсутствие фильтров по периодам в лидерборде (Phase 2).
* Отсутствие экспорта данных (Phase 2).
* Отсутствие метрик и мониторинга (Phase 2).
* Отсутствие rate limiting (добавится при росте нагрузки).
* Long Polling вместо Webhook (переход при масштабировании).
* In-memory сессии вместо Redis (переход при масштабировании или необходимости horizontal scaling).
* Простая формула настроения питомца (может быть улучшена в будущем).

---

**Документ готов к передаче команде разработки.**

**Версия**: 1.1  
**Дата**: 2025-11-21  
**Статус**: Утверждено  
**Изменения в 1.1**: Обновлена архитектура - переход на модульный монолит (FamilyTaskManager.Host), реорганизация тестов по типам.

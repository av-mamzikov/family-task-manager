# Руководство по созданию задач через бота

## Обзор

Реализована функциональность создания задач через Telegram-бота. Поддерживаются два типа задач:
- **Разовые задачи** - выполняются один раз с указанным сроком
- **Периодические задачи** - повторяются по расписанию (Quartz Cron)

## Процесс создания задачи

### 1. Начало создания

Пользователь может начать создание задачи несколькими способами:
- Через команду в настройках семьи
- Через callback кнопку "Создать задачу" (`create_task`)

### 2. Выбор типа задачи

Бот предлагает выбрать тип задачи:
- 📝 **Разовая задача** - для однократного выполнения
- 🔄 **Периодическая задача** - для регулярного повторения

### 3. Ввод названия

Пользователь вводит название задачи:
- Минимум 3 символа
- Максимум 100 символов
- Примеры: "Покормить кота", "Выгулять собаку", "Почистить клетку"

### 4. Ввод очков

Пользователь указывает количество очков за выполнение:
- От 1 до 100
- Чем сложнее задача, тем больше очков

### 5. Выбор питомца

Бот показывает список всех питомцев семьи:
- Каждая задача обязательно привязывается к питомцу
- Выполнение задачи влияет на настроение выбранного питомца

### 6. Дополнительные параметры

#### Для разовой задачи:
Пользователь вводит срок выполнения в днях:
- `0` - сегодня
- `1` - завтра
- `7` - через неделю
- `30` - через месяц

#### Для периодической задачи:
Пользователь вводит расписание в формате Quartz Cron:

**Примеры cron-выражений:**
- `0 0 9 * * ?` - ежедневно в 9:00
- `0 0 20 * * ?` - ежедневно в 20:00
- `0 0 9 */5 * ?` - каждые 5 дней в 9:00
- `0 0 9 * * MON` - каждый понедельник в 9:00
- `0 0 12 1 * ?` - первого числа каждого месяца в 12:00

**Формат Quartz Cron:**
```
секунды минуты часы день месяц день_недели [год]
```

### 7. Подтверждение

После ввода всех данных:
- Разовая задача создается немедленно и становится доступной всем участникам
- Периодическая задача создается как шаблон, экземпляры будут создаваться автоматически по расписанию

## Технические детали

### Conversation States

Процесс создания задачи использует следующие состояния сессии:
1. `AwaitingTaskType` - ожидание выбора типа задачи (не используется, выбор через callback)
2. `AwaitingTaskTitle` - ожидание ввода названия
3. `AwaitingTaskPoints` - ожидание ввода очков
4. `AwaitingTaskPetSelection` - ожидание выбора питомца (через inline кнопки)
5. `AwaitingTaskDueDate` - ожидание ввода срока (для разовых задач)
6. `AwaitingTaskSchedule` - ожидание ввода расписания (для периодических задач)

### Callback Actions

- `create_task` - начало создания задачи
- `select_tasktype_onetime` - выбор разовой задачи
- `select_tasktype_recurring` - выбор периодической задачи
- `taskpet_{petId}` - выбор питомца для задачи

### Use Cases

**Для разовых задач:**
- `CreateTaskCommand` - создает `TaskInstance` с типом `OneTime`

**Для периодических задач:**
- `CreateTaskTemplateCommand` - создает `TaskTemplate` с расписанием
- Worker автоматически создает `TaskInstance` по расписанию через `CreateTaskInstanceFromTemplateCommand`

### Валидация

**Название задачи:**
- Не пустое
- От 3 до 100 символов

**Очки:**
- Целое число
- От 1 до 100

**Срок выполнения (разовые):**
- Целое число дней
- От 0 до 365

**Расписание (периодические):**
- Не пустое
- Валидное Quartz Cron выражение (проверяется при создании шаблона)

**Питомец:**
- Должен существовать
- Должен принадлежать текущей семье

## Примеры использования

### Пример 1: Разовая задача
```
Пользователь: /tasks → Создать задачу
Бот: Выберите тип задачи
Пользователь: [Разовая задача]
Бот: Введите название
Пользователь: Купить корм для кота
Бот: Введите очки
Пользователь: 15
Бот: Выберите питомца
Пользователь: [🐱 Мурзик]
Бот: Введите срок в днях
Пользователь: 1
Бот: ✅ Задача "Купить корм для кота" создана!
     💯 Очки: 15
     📅 Срок: 22.11.2025 19:28
```

### Пример 2: Периодическая задача
```
Пользователь: /tasks → Создать задачу
Бот: Выберите тип задачи
Пользователь: [Периодическая задача]
Бот: Введите название
Пользователь: Покормить кота
Бот: Введите очки
Пользователь: 10
Бот: Выберите питомца
Пользователь: [🐱 Мурзик]
Бот: Введите расписание (Quartz Cron)
Пользователь: 0 0 9 * * ?
Бот: ✅ Периодическая задача "Покормить кота" создана!
     💯 Очки: 10
     🔄 Расписание: 0 0 9 * * ?
     
     Задача будет автоматически создаваться по расписанию.
```

## Права доступа

Согласно ТЗ MVP1:
- **Разовые задачи** - могут создавать Админ и Взрослый
- **Периодические задачи** - может создавать только Админ

> ⚠️ **Примечание:** В текущей реализации проверка прав доступа не реализована. Это нужно добавить в следующей итерации.

## Что дальше

После создания задачи:
1. Задача появляется в списке активных задач (`/tasks`)
2. Участники могут взять задачу в работу
3. После выполнения начисляются очки и обновляется настроение питомца
4. Отправляются уведомления всем участникам семьи

## Известные ограничения

1. Нет проверки прав доступа (Админ/Взрослый для разовых, только Админ для периодических)
2. Нет предпросмотра расписания для периодических задач
3. Нет возможности выбрать конкретное время для разовых задач (только дни)
4. Нет валидации cron-выражения на стороне бота (только при создании в БД)

## Файлы

**Bot Handlers:**
- `src/FamilyTaskManager.Host/Modules/Bot/Handlers/CommandHandler.cs` - обработка ввода текста
- `src/FamilyTaskManager.Host/Modules/Bot/Handlers/CallbackQueryHandler.cs` - обработка кнопок

**Models:**
- `src/FamilyTaskManager.Host/Modules/Bot/Models/UserSession.cs` - состояния диалога

**Use Cases:**
- `src/FamilyTaskManager.UseCases/Tasks/CreateTask.cs` - создание разовой задачи
- `src/FamilyTaskManager.UseCases/Tasks/CreateTaskTemplate.cs` - создание шаблона периодической задачи

# Система Invite-кодов

## Обзор

Реализована полная система приглашений для добавления участников в семью через уникальные коды-приглашения.

## Архитектура

### Core Layer

**`Invitation` Entity** (`src/FamilyTaskManager.Core/FamilyAggregate/Invitation.cs`)
- Уникальный 8-символьный код (без двусмысленных символов)
- Привязка к семье и роли
- Срок действия (по умолчанию 7 дней)
- Статус активности (одноразовое использование)
- Методы валидации: `IsValid()`, `IsExpired()`, `Deactivate()`

### Infrastructure Layer

**`InvitationConfiguration`** (`src/FamilyTaskManager.Infrastructure/Data/Config/InvitationConfiguration.cs`)
- Конфигурация EF Core для таблицы Invitations
- Индексы для быстрого поиска по коду
- Уникальный индекс на поле Code

**Migration**: `AddInvitationsTable`
- Создана миграция для добавления таблицы Invitations в БД

### Use Cases Layer

**`CreateInviteCodeCommand`** (`src/FamilyTaskManager.UseCases/Families/CreateInviteCode.cs`)
- Создание invite-кода для семьи
- Проверка прав доступа (только Admin)
- Генерация уникального кода
- Настраиваемый срок действия

**`JoinByInviteCodeCommand`** (`src/FamilyTaskManager.UseCases/Families/JoinByInviteCode.cs`)
- Присоединение к семье по коду
- Валидация приглашения (активность, срок действия)
- Проверка на дублирование участников
- Автоматическая деактивация кода после использования

**`GetInvitationByCodeSpec`** (`src/FamilyTaskManager.UseCases/Families/Specifications/GetInvitationByCodeSpec.cs`)
- Спецификация для поиска приглашения по коду

### Bot Handlers

**`CallbackQueryHandler`** (обновлен)
- Обработка callback `family_invite_{familyId}` - показ выбора роли
- Обработка callback `invite_role_{familyId}_{role}` - создание invite-кода
- Генерация Telegram deep link: `https://t.me/{botUsername}?start=invite_{code}`

**`CommandHandler`** (обновлен)
- Обработка `/start invite_{code}` - присоединение к семье
- Приветственное сообщение с информацией о семье и роли

**`FamilyCommandHandler`** (уже был готов)
- Кнопка "🔗 Создать приглашение" для админов в `/family`

## Использование

### Для администратора семьи

1. Открыть `/family`
2. Нажать "🔗 Создать приглашение"
3. Выбрать роль для нового участника (Admin/Adult/Child)
4. Получить ссылку-приглашение вида: `https://t.me/BotUsername?start=invite_ABC12345`
5. Отправить ссылку человеку, которого хотите пригласить

### Для приглашенного пользователя

1. Перейти по ссылке-приглашению
2. Нажать "Start" в Telegram
3. Автоматически присоединиться к семье с указанной ролью
4. Получить приветственное сообщение

## Безопасность и валидация

### Проверки при создании invite-кода

- ✅ Семья существует
- ✅ Создатель является членом семьи
- ✅ Создатель имеет роль Admin
- ✅ Генерация уникального кода

### Проверки при использовании invite-кода

- ✅ Пользователь существует
- ✅ Приглашение существует
- ✅ Приглашение активно
- ✅ Приглашение не истекло
- ✅ Семья существует
- ✅ Пользователь еще не является членом семьи

### Особенности

- **Одноразовое использование**: После успешного присоединения код автоматически деактивируется
- **Срок действия**: По умолчанию 7 дней, настраивается при создании
- **Уникальность**: Каждый код уникален, используются только однозначные символы (без 0, O, 1, I, L)
- **Роли**: Можно создавать приглашения для любой роли (Admin, Adult, Child)

## Тесты

### Unit Tests

**`InvitationTests`** (`tests/FamilyTaskManager.UnitTests/Core/FamilyAggregate/InvitationTests.cs`)
- Тесты доменной модели Invitation
- Проверка генерации кода
- Проверка валидации и истечения срока

**`CreateInviteCodeHandlerTests`** (`tests/FamilyTaskManager.UnitTests/UseCases/Families/CreateInviteCodeHandlerTests.cs`)
- Успешное создание кода админом
- Отказ для не-админов
- Отказ для не-членов семьи
- Проверка срока действия

**`JoinByInviteCodeHandlerTests`** (`tests/FamilyTaskManager.UnitTests/UseCases/Families/JoinByInviteCodeHandlerTests.cs`)
- Успешное присоединение по валидному коду
- Отказ для истекших кодов
- Отказ для неактивных кодов
- Отказ для уже существующих членов
- Деактивация кода после использования

## База данных

### Таблица Invitations

```sql
CREATE TABLE Invitations (
    Id UUID PRIMARY KEY,
    FamilyId UUID NOT NULL,
    Role VARCHAR(20) NOT NULL,
    Code VARCHAR(20) NOT NULL UNIQUE,
    CreatedAt TIMESTAMP NOT NULL,
    ExpiresAt TIMESTAMP NULL,
    IsActive BOOLEAN NOT NULL DEFAULT TRUE,
    CreatedBy UUID NOT NULL,
    INDEX idx_invitations_code (Code),
    INDEX idx_invitations_family (FamilyId),
    INDEX idx_invitations_active_expires (IsActive, ExpiresAt)
);
```

## Следующие шаги

### Рекомендуемые улучшения

1. **Получение BotUsername из конфигурации**
   - Сейчас используется заглушка "YourBotUsername"
   - Нужно добавить в appsettings.json

2. **Управление приглашениями**
   - Просмотр активных приглашений
   - Отзыв приглашений
   - История использования

3. **Уведомления**
   - Уведомление админа о присоединении нового участника
   - Уведомление всей семьи о новом члене

4. **Статистика**
   - Количество созданных приглашений
   - Количество использованных приглашений
   - Конверсия приглашений

5. **Ограничения**
   - Лимит активных приглашений на семью
   - Лимит приглашений в день

## Миграция

Для применения изменений в БД:

```bash
cd src/FamilyTaskManager.Infrastructure
dotnet ef database update --context AppDbContext
```

## Проверка работоспособности

1. Запустить систему: `.\scripts\start-system.ps1`
2. Открыть бота в Telegram
3. Создать семью через `/start`
4. Открыть `/family`
5. Нажать "🔗 Создать приглашение"
6. Выбрать роль
7. Скопировать ссылку и открыть в другом аккаунте
8. Проверить успешное присоединение

## Файлы

### Созданные файлы

- `src/FamilyTaskManager.Core/FamilyAggregate/Invitation.cs`
- `src/FamilyTaskManager.Infrastructure/Data/Config/InvitationConfiguration.cs`
- `src/FamilyTaskManager.UseCases/Families/CreateInviteCode.cs`
- `src/FamilyTaskManager.UseCases/Families/JoinByInviteCode.cs`
- `src/FamilyTaskManager.UseCases/Families/Specifications/GetInvitationByCodeSpec.cs`
- `tests/FamilyTaskManager.UnitTests/Core/FamilyAggregate/InvitationTests.cs`
- `tests/FamilyTaskManager.UnitTests/UseCases/Families/CreateInviteCodeHandlerTests.cs`
- `tests/FamilyTaskManager.UnitTests/UseCases/Families/JoinByInviteCodeHandlerTests.cs`
- `src/FamilyTaskManager.Infrastructure/Data/Migrations/[timestamp]_AddInvitationsTable.cs`

### Измененные файлы

- `src/FamilyTaskManager.Infrastructure/Data/AppDbContext.cs` - добавлен DbSet<Invitation>
- `src/FamilyTaskManager.Host/Modules/Bot/Handlers/CallbackQueryHandler.cs` - добавлена обработка invite callbacks
- `src/FamilyTaskManager.Host/Modules/Bot/Handlers/CommandHandler.cs` - реализована обработка invite в /start

## Статус

✅ **Полностью реализовано и готово к использованию**

- ✅ Доменная модель
- ✅ EF Core конфигурация
- ✅ Use Cases
- ✅ Миграция БД
- ✅ Bot handlers
- ✅ Unit тесты
- ✅ Документация

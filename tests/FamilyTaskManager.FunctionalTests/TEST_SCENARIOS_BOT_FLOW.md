# Тестовые сценарии для Bot Flow (Telegram Bot уровень)

> Эти сценарии описывают **сквозные пользовательские истории через Telegram-бота**:
>
> Update (сообщение / callback) → `MessageHandler` / другие bot handlers → UseCases → ответы бота.
>
> В тестах используется `TestTelegramBotClient` для перехвата всех отправленных сообщений и inline- / reply-клавиатур.

## 1. Регистрация и первый запуск (`/start`)

### TS-BOT-001. Первый запуск бота новым пользователем

- **Цель**: проверить базовый онбординг и регистрацию через `/start`.
- **Предусловия**:
    - В БД нет пользователя с данным `telegramId`.
    - У пользователя нет семей.
- **Шаги**:
    - Сформировать `Message` с текстом `/start`.
    - Вызвать `MessageHandler.HandleCommandAsync(testBotClient, message, ct)`.
    - Взять последнее сообщение для этого `chatId` через `TestTelegramBotClient.GetLastMessageTo`.
- **Ожидаемый результат**:
    - В БД создан пользователь (проверяется через контекст или use case уровня UseCases).
    - Сообщение содержит приветственный текст (`BotConstants.Messages.WelcomeMessage`).
    - В конце текста присутствует блок `NoFamiliesJoin` с предложением создать семью.
    - В ответе есть **inline-клавиатура** с кнопкой `"➕ Создать семью"` и callback-data `"create_family"`.

---

### TS-BOT-002. Первый запуск по инвайт-ссылке `/start invite_CODE`

- **Цель**: присоединение к семье по invite-коду через `/start`.
- **Предусловия**:
    - В БД существует активный invite-код `code`, выданный какой-либо семье.
    - Пользователь с данным `telegramId` ещё не зарегистрирован.
- **Шаги**:
    - Отправить команду `/start invite_code` (где `invite_code` в формате `invite_XXXX`).
    - Вызвать `HandleCommandAsync`.
    - Считать последнее сообщение из `SentMessages`.
- **Ожидаемый результат**:
    - Пользователь зарегистрирован.
    - Выполнена команда `JoinByInviteCodeCommand` и она успешна.
    - Бот отправляет сообщение вида `"🎉 Добро пожаловать в семью!"` + текст из
      `BotConstants.Messages.FamilyJoined(...)`.
    - После успешного присоединения вызывается `SendMainMenuAsync`, то есть отправляется сообщение с текстом,
      оканчивающимся на `"🏠 Главное меню"` и **reply-клавиатурой** главного меню.

---

### TS-BOT-003. `/start` для уже существующего пользователя с семьями

- **Цель**: проверить поведение `/start`, когда пользователь уже зарегистрирован и состоит в семье.
- **Предусловия**:
    - Пользователь уже зарегистрирован в системе.
    - `GetUserFamiliesQuery` для него возвращает хотя бы одну семью.
- **Шаги**:
    - Отправить `/start`.
    - Вызвать `HandleCommandAsync`.
    - Получить последнее сообщение.
- **Ожидаемый результат**:
    - В `UserSession.CurrentFamilyId` устанавливается первая семья из списка.
    - Бот отправляет сообщение: приветствие + `FamilyJoined(family.Name, roleText)`.
    - После этого отображается главное меню (reply-клавиатура от `MainMenuHelper.GetMainMenuKeyboard()`).

---

## 2. Главное меню и навигация

### TS-BOT-010. Отображение главного меню после успешного `/start`

- **Цель**: убедиться, что главное меню соответствует концепции MVP1.
- **Предусловия**:
    - Пользователь успешно прошёл `/start` и имеет активную семью.
- **Шаги**:
    - Использовать сценарий TS-BOT-003.
    - Взять последнее сообщение и проверить `ReplyMarkup`.
- **Ожидаемый результат**:
    - Reply-клавиатура содержит кнопки:
        - `"🏠 Семья"`
        - `"✅ Мои задачи"`
        - `"🐾 Питомец"`
        - `"⭐ Мои очки"`
        - `"📊 Статистика"`

---

### TS-BOT-011. Навигация по меню кнопками (без команд `/...`)

- **Цель**: проверить, что нажатия на кнопки меню эквивалентны вызовам соответствующих команд.
- **Предусловия**:
    - Пользователь имеет активную семью и находится в главном меню.
- **Шаги (пример для нескольких кнопок)**:
    - Отправить сообщение `"🏠 Семья"` → `HandleCommandAsync`.
    - Отправить `"✅ Мои задачи"` → `HandleCommandAsync`.
    - Отправить `"🐾 Питомец"` → `HandleCommandAsync`.
    - Отправить `"📊 Статистика"` → `HandleCommandAsync`.
- **Ожидаемый результат**:
    - Для каждой кнопки вызывается соответствующий handler (`FamilyCommandHandler`, `TasksCommandHandler`,
      `PetCommandHandler`, `StatsCommandHandler`).
    - При ошибке регистрации пользователя для любой из кнопок бот отвечает `"❌ Ошибка. Попробуйте /start"`.

---

### TS-BOT-012. Команда `/help`

- **Цель**: проверить справку по командам.
- **Предусловия**: пользователь зарегистрирован.
- **Шаги**:
    - Отправить `/help`.
    - Вызвать `HandleCommandAsync`.
    - Получить последнее сообщение.
- **Ожидаемый результат**:
    - Сообщение начинается с `"📖 Справка по командам:"` и содержит текст `BotConstants.Help.Commands`.
    - В конце присутствует фраза про использование кнопок меню.

---

### TS-BOT-013. Неизвестная команда

- **Цель**: обработка несуществующих `/`-команд.
- **Предусловия**: пользователь зарегистрирован.
- **Шаги**:
    - Отправить `/unknown_command`.
    - Вызвать `HandleCommandAsync`.
- **Ожидаемый результат**:
    - Бот отвечает текстом `BotConstants.Errors.UnknownCommand`.

---

## 3. Работа с семьёй через бота

> Детализация сценариев `/family` в значительной степени ложится на `FamilyCommandHandler`, но на уровне Bot Flow мы
> проверяем интеграцию и маршрутизацию.

### TS-BOT-020. Команда `/family` при отсутствии семей

- **Цель**: проверить, что бот корректно ведёт пользователя к созданию семьи, если семей нет.
- **Предусловия**:
    - Пользователь зарегистрирован.
    - `GetOrRegisterUserAsync` успешен.
    - Для пользователя нет семей (может обрабатываться внутри `FamilyCommandHandler`).
- **Шаги**:
    - Отправить `/family`.
    - Вызвать `HandleCommandAsync`.
    - Проанализировать сообщения, отправленные соответствующим handler-ом (через `TestTelegramBotClient`).
- **Ожидаемый результат**:
    - При успехе регистрации вызывается `FamilyCommandHandler.HandleAsync`.
    - Бот показывает пользователю информацию о том, что семей нет, и предлагает создать семью (конкретный текст зависит
      от handler-а).

---

### TS-BOT-021. Команда `/family` с несколькими семьями пользователя

- **Цель**: проверить отображение списка семей и выбор активной семьи.
- **Предусловия**:
    - Пользователь состоит в нескольких семьях.
    - `FamilyCommandHandler` реализует логику выбора/переключения семей.
- **Шаги**:
    - Отправить `/family`.
    - Вызвать `HandleCommandAsync`.
    - Проверить текст и inline-клавиатуру в ответе.
- **Ожидаемый результат** (в общих чертах):
    - Сообщение содержит список семей с ролями.
    - Inline-клавиатура позволяет выбрать активную семью.
    - После выбора в сессии обновляется `CurrentFamilyId`, а главное меню и другие команды работают в контексте новой
      семьи.

---

## 4. Список задач и выполнение через бот

> Здесь сценарии ориентируются на команды `/tasks` и соответствующий `TasksCommandHandler`, который взаимодействует с
> UseCases задач.

### TS-BOT-030. Просмотр списка задач через `/tasks`

- **Цель**: пользователь видит свои/семейные задачи.
- **Предусловия**:
    - У пользователя есть активная семья (`CurrentFamilyId` заполнен).
    - В семье есть несколько активных задач.
- **Шаги**:
    - Отправить `/tasks`.
    - Вызвать `HandleCommandAsync`.
    - Получить последнее сообщение.
- **Ожидаемый результат** (на уровне Bot Flow):
    - При успешной регистрации пользователя вызывается `TasksCommandHandler.HandleAsync`.
    - Сообщение содержит текст со списком доступных задач (формат зависит от handler-а).
    - В сообщении присутствуют inline-кнопки для действий над задачами (например, взять/выполнить).

---

### TS-BOT-031. Взятие задачи в работу через inline-кнопку

- **Цель**: проверить, что после нажатия на кнопку для задачи изменяется её статус и UI.
- **Предусловия**:
    - Есть хотя бы одна активная задача в семье.
    - Список задач отображён с inline-кнопкой вида "✋ Взять" (или аналогичной).
- **Шаги (уровень Bot Flow)**:
    - Создать `CallbackQuery` с нужным `callback_data` для взятия задачи.
    - Передать `Update` с этим callback в соответствующий update-handler (не `MessageHandler`, а callback-handler модуля
      бота).
    - Использовать `TestTelegramBotClient` для перехвата ответов.
- **Ожидаемый результат**:
    - Задача помечается как "в работе" в БД (через UseCase `TakeTaskCommand`).
    - Бот редактирует или отправляет сообщение с подтверждением и обновлённым статусом задачи.

---

### TS-BOT-032. Выполнение задачи через inline-кнопку

- **Цель**: проверить полный цикл от выбора задачи до её завершения.
- **Предусловия**:
    - Есть задача, взятая данным пользователем в работу.
- **Шаги**:
    - Отправить callback для действия "✅ Выполнить" (формат callback зависит от handler-а).
    - Обработать update соответствующим callback-handler-ом.
    - Проверить сообщения в `SentMessages`.
- **Ожидаемый результат**:
    - Задача в БД имеет статус `Completed`, пользователю начислены очки.
    - Бот отправляет сообщение с подтверждением выполнения и информацией об очках / состоянии питомца (в рамках MVP1 как
      минимум подтверждение и обновлённое состояние).

---

## 5. Питомцы и настроение через бот

### TS-BOT-040. Просмотр информации о питомце (`/pet` или кнопка "🐾 Питомец")

- **Цель**: пользователь видит своих питомцев и их настроение.
- **Предусловия**:
    - В активной семье есть как минимум один питомец с задачами.
- **Шаги**:
    - Отправить `/pet` или сообщение `"🐾 Питомец"`.
    - Вызвать `HandleCommandAsync`.
    - Проверить текст ответа.
- **Ожидаемый результат**:
    - При успехе регистрации вызывается `PetCommandHandler.HandleAsync`.
    - Сообщение содержит список питомцев и их базовую информацию (имя, тип, настроение/состояние).

---

## 6. Статистика и очки через бот

### TS-BOT-050. Просмотр статистики семьи (`/stats` или "📊 Статистика")

- **Цель**: проверить вызываемость `StatsCommandHandler` и базовый вывод статистики.
- **Предусловия**:
    - У семьи есть выполненные задачи/очki.
- **Шаги**:
    - Отправить `/stats` или сообщение `"📊 Статистика"`.
    - Вызвать `HandleCommandAsync`.
- **Ожидаемый результат**:
    - При успехе регистрации вызывается `StatsCommandHandler.HandleAsync`.
    - Сообщение содержит сводную информацию (лидерборд, историю или заглушку — в зависимости от реализации handler-а).

---

### TS-BOT-051. Попытка посмотреть "⭐ Мои очки" без активной семьи

- **Цель**: проверить ошибку при отсутствии активной семьи.
- **Предусловия**:
    - `UserSession.CurrentFamilyId == null`.
- **Шаги**:
    - Отправить сообщение `"⭐ Мои очки"`.
    - Вызвать `HandleCommandAsync`.
    - Получить последнее сообщение.
- **Ожидаемый результат**:
    - Бот отвечает текстом `"❌ Сначала выберите активную семью через /family"`.

---

### TS-BOT-052. Просмотр "⭐ Мои очки" при выбранной семье

- **Цель**: проверить текущую реализацию заглушки для очков.
- **Предусловия**:
    - `UserSession.CurrentFamilyId` установлен.
- **Шаги**:
    - Отправить `"⭐ Мои очки"`.
    - Вызвать `HandleCommandAsync`.
- **Ожидаемый результат**:
    - Бот отвечает текстом `"⭐ Мои очки\n(В разработке)"`.

---

## 7. Управление состоянием диалога (conversation)

> Эти сценарии проверяют логику обработки команд `"❌ Отменить"`, `"/cancel"` и `"⬅️ Назад"` при активном состоянии
> разговора.

### TS-BOT-060. Отмена текущего сценария (`"❌ Отменить"` или `/cancel`)

- **Цель**: убедиться, что глобальная отмена корректно завершает conversation и возвращает пользователя в главное меню.
- **Предусловия**:
    - В `UserSession.State` установлено значение, отличное от `ConversationState.None` (пользователь в процессе
      сценария, например создания семьи или задачи).
- **Шаги**:
    - Отправить текст `"❌ Отменить"` **или** `/cancel`.
    - Вызвать `HandleCommandAsync`.
- **Ожидаемый результат**:
    - Вызывается `conversationRouter.HandleCancelConversationAsync`, в котором состояние диалога сбрасывается.
    - Коллбек для возврата (`SendMainMenuAsync`) вызывается, бот отправляет сообщение с `"🏠 Главное меню"` и
      reply-клавиатурой.

---

### TS-BOT-061. Переход назад по сценарию (`"⬅️ Назад"`)

- **Цель**: проверить, что кнопка "Назад" переносит пользователя к предыдущему шагу диалога.
- **Предусловия**:
    - Пользователь находится в середине какого-либо сценария (state != None и есть история шагов внутри router-а).
- **Шаги**:
    - Отправить текст `"⬅️ Назад"`.
    - Вызвать `HandleCommandAsync`.
- **Ожидаемый результат**:
    - Вызывается `conversationRouter.HandleBackInConversationAsync`.
    - В зависимости от реализации router-а пользователь видит предыдущий вопрос/шаг.

---

## 8. Общие требования к Bot Flow тестам

- **Инструменты**:
    - Использовать `TestTelegramBotClient` для перехвата всех сообщений и callback-ответов.
    - Для генерации входящих `Update` можно использовать отдельные фабрики (например, `UpdateFactory`) или вручную
      конструировать `Message`/`CallbackQuery`.
- **Покрытие**:
    - Каждый сценарий из MVP1, проходящий через бот, должен быть отражён либо как отдельный тест, либо как группа тестов
      с общей преамбулой.
- **Изоляция**:
    - Все Bot Flow тесты должны работать с тестовой БД и чистым состоянием (через test host / WebApplicationFactory и
      Testcontainers).

